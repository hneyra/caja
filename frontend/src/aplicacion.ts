/**
 * Como se llama esta aplicacion, en un solo sitio.
 *
 * La pagina lo pinta y la prueba lo busca: si el nombre se escribiera dos veces, la prueba
 * podria seguir en verde con la pagina diciendo otra cosa.
 */
export const NOMBRE_DE_LA_APLICACION = "caja-web";

/** El modulo del manual al que sirve esta interfaz. Es texto: lleva tilde. */
export const MODULO = "Tesorería";

/**
 * La entidad es un dato del despliegue, no una constante del codigo. Hasta que llegue de
 * donde tenga que llegar, aqui esta la del diseno (`TesoreriaV6.dc.html`, prop `entidad`).
 */
export const ENTIDAD = "Municipalidad Distrital de Catacaos";
