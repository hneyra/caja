# `caja` — Contexto para agentes

Órdenes de cobro, recibo, turno, arqueo, cierre y medios de pago. **No sabe qué es un tributo**, y
por eso sirve para cobrar un puesto de mercado o un nicho.

Uno de los cinco repositorios de **Kamayuk**, el producto multi-municipal que reimplementa el
sistema documentado en el manual de usuario del SGTM de la Municipalidad Provincial de Sullana.
El reparto lo decide
[ADR-0029](https://github.com/hneyra/infrastructure/blob/main/docs/30-arquitectura/adr/ADR-0029-cuatro-sistemas-separados.md);
qué tabla fue a qué repositorio y por qué, [GOB-05](https://github.com/hneyra/sgtm/blob/migracion-a-microservicios/docs/00-gobierno/inventario-del-corte.md).

## Qué hay hoy, medido y no supuesto

Desde **P5D** (2026-09-04) este repositorio tiene su negocio dentro. Lo que cada pieza vale está
medido en [`docs/00-gobierno/P5D-extraccion.md`](docs/00-gobierno/P5D-extraccion.md); esta tabla es
el resumen.

| Pieza | Estado |
|---|---|
| `backend/kamayuk-caja-esquema` | **`V1__baseline.sql`** (23 tablas) y **`V2__ordenes_de_cobro_y_outbox.sql`**. El baseline se corrigió al traerlo: le sobraban **ocho funciones y cinco dominios** de otros sistemas, y una de las ocho hacía morir la migración sobre una base sin `unaccent` |
| `backend/kamayuk-caja-nucleo` | El contexto acotado entero: orden de cobro, ventanilla, recibo, turno, cierre, arqueo, tasas y **el buzón de salida** |
| `backend/kamayuk-caja-{dominio-compartido, plataforma}` | Copias de las de `rentas`, con el paquete renombrado. Ver el hueco de las cuatro copias |
| `backend/kamayuk-caja-aplicacion` | Ensambla y aloja las barreras |
| `infrastructure/` — el descriptor | `yarn verificar` en verde, **31 pruebas**. Declara **tres imágenes** y despliega **dos cosas**: el backend con el perfil `web` y, desde #17, `kamayuk-caja-interfaz` —su `ConfigMap` con el `nginx.conf` del repositorio, su `Deployment` sin una sola variable de entorno y su `Service`—. Su `IngressRoute` va **partido en dos rutas con `priority` explícita**: `/caja/api/v1` al backend y `/caja` a la interfaz, quitándole el prefijo con un `stripPrefix`. **El responsable de la conciliación ya no falta**: C-7 le dio su campo a `EntornoDelDescriptor` y la línea 170 pone `KAMAYUK_CAJA_RESPONSABLE` desde `e.operacion.responsable` — este renglón decía lo contrario y era de antes de C-7. Lo que falta hoy vive en el otro repositorio, y está declarado en [`docs/00-gobierno/huecos-en-infrastructure.md`](docs/00-gobierno/huecos-en-infrastructure.md) |
| `docs/30-arquitectura/adr/` | Sin ningún ADR propio, y sigue siendo correcto: lo que la caja hace lo deciden ADR-0026 y ADR-0029 |
| `frontend/` — `caja-web` | **El andamio, desde #3**: React 19 + Vite 6, un solo paquete y cero dependencias de red. `yarn verificar` encadena ESLint —con sus tres reglas propias y una muestra por regla—, `tsc` y Vitest; `yarn build` produce `frontend/dist/`. Desde #4 los **tokens de V6** en `src/ds/`, y desde #5 **todo lo que las pantallas van a enseñar** en `src/datos/`, tipado y copiado del artboard. Desde #6 lo primero que se dibuja: la **barra global** de 52 px en `src/barra/`, con su aviso de servicio y su toast; desde #7 el **árbol de módulos** de 252 px en `src/arbol/`, con su filtro y su cola de trabajo; desde #8 el **marco** en `src/marco/`: pestañas, hash en la URL, el hueco de «No hay ningún submódulo abierto», la tarjeta de un submódulo ajeno y el diálogo de cambios sin guardar; y desde #9 la **paleta de comandos** en `src/paleta/`, con el lanzador y el menú de sesión. Desde **#10 hay la primera de las cuatro pantallas**: `#panel`, el Panel de Tesorería, en `src/pantallas/`; y desde **#11, #12 y #13, la segunda entera**: `#recibos`, con la lista de 376 px —búsqueda, chips, orden y vacío— y a su derecha la **ficha de un recibo existente** —cabecera, cinco secciones, el renderizador de las seis clases de campo y su tabla de cuotas— y el **cobro nuevo**: la barra de caja y contribuyente, el número que se emitirá, la validación que **bloquea** cuando la caja está cerrada, el contador de pendientes de cada pestaña, el resumen «Lo que se va a registrar» y la emisión. `#recibos` ya no tiene ningún marcador. Y desde **#14 están las cuatro**: `#cajas`, con los seis nodos de 300 px y el arqueo del elegido a su derecha —cabecera pegajosa incluida—, y `#tarifario`, con sus tres pestañas de consulta, su píldora «Solo lectura» y la nota de cabecera y la de pie de cada una. Con eso **el marcador de sección desaparece**: el `switch` de `PantallaDeSeccion` —que es lo que `App` recibe por omisión en su ranura `Pantalla`— cubre la unión entera sin `default`, y `src/marco/MarcadorDeSeccion.tsx` pasa a ser `src/marco/pantalla.ts`, sólo el contrato. Y desde **#15 está la envoltura**: la impresión en A4 vertical —`@media print` con su `@page`, y las cuatro piezas del marco marcadas `data-cromo`—, el anillo de foco que **no llegaba a ningún campo**, y los **cuatro arneses de navegador** en CI: `paleta.mjs` (#9), `pegajosa.mjs` (#14), `mirar.mjs` y `cero-red.mjs`. Los cortes responsive no hubo que escribirlos: entraron con `global.css` en #4 y aquí se **miden**. **Está entero**, y `yarn verificar` da `Test Files 20 passed (20)` · `Tests 634 passed (634)`. **Y NO SE CONECTA AL BACKEND, que es la mitad que hay que leer.** No es que le falte: es que no lo tiene y no lo quiere todavía. Los datos salen de `src/datos/`, copiados del artboard; `eslint.config.mjs` prohíbe `fetch` y `XMLHttpRequest` en el código; `frontend/nginx.conf` **no reenvía a ningún sitio** —contar la directiva de reenvío da cero— y `vite.config.ts` tampoco declara `server.proxy`; y `verificaciones/cero-red.mjs` lo mide en un Chromium de verdad: **0 peticiones de conexión y 0 a terceros sin declarar**, con las 10 de Source Sans 3 nombradas como lo que son. Lo que compra es que la ventanilla dibuje su pantalla en un municipio sin salida a internet; lo que cuesta es que **la ficha de sesión sea decorativa**: no hay token, y por eso esta interfaz tampoco tiene *client* en Keycloak (ver el documento de huecos) |
| La imagen de la interfaz | **`kamayuk-caja-interfaz`, desde #16.** `frontend/Dockerfile` en dos etapas —Node 22 que compila, `nginx:1.31.4-alpine` que sirve `dist/` con `USER 101` y `HEALTHCHECK`—, su `frontend/nginx.conf` **sin ningún reenvío al backend**, su `.dockerignore`, y la tercera entrada de la matriz de `publicar-imagenes.yml`, con su `file` y su `context` propios. **No se llama `caja-web`**: ese nombre ya es el `Deployment` y el `Service` del backend con el perfil `web` de Spring. **Desde #17 el descriptor la despliega y desde #18 el compose la levanta**, en el servicio `caja-interfaz`. Y falta también lo que sólo `frontend/` puede hacer: **la interfaz no es alcanzable bajo `/caja` todavía**, porque su `index.html` fija los recursos en la raíz del dominio (`vite.config.ts` no declara `base`) y `BarraGlobal.tsx` escribe `/escudo-catacaos.png` en el JSX, que Vite no reescribe. Está medido en la fila de #17, y la guarda que obliga a mover las dos a la vez vive en `descriptor.test.ts` |
| `despliegue/compose.yaml` | **Cuatro servicios desde #18**: `caja-migraciones`, `caja-implantacion`, `caja` y `caja-interfaz`. El cuarto construye `frontend/Dockerfile` con contexto `../frontend`, **no declara `depends_on`** —esta interfaz no necesita el backend, y una dependencia inventada haría que el compose mintiera sobre el grafo que la guarda de `infrastructure` compara— y es **el único que publica un puerto**, `${KAMAYUK_PUERTO_INTERFAZ_CAJA:-8082}`, porque bajo `/caja` todavía no se llega (#37) |
| Lo que falta y vive en `infrastructure` | **Declarado, no descubierto tarde**: [`docs/00-gobierno/huecos-en-infrastructure.md`](docs/00-gobierno/huecos-en-infrastructure.md) — la `version` del descriptor fijado, las dos guardas que este lote deja en rojo y el *client* de Keycloak que esta interfaz no tiene |

**Tres roles y cero extensiones.** `crear-roles.sql` declara `kamayuk_owner`, `kamayuk_app` y
`kamayuk_readonly` — `rol_carga_parametros` es de `normativa` y aquí no recibe un solo `GRANT`— y
**ninguna extensión de PostgreSQL**. No es limpieza: una ventanilla cuya base necesita PostGIS no se
levanta en cualquier sitio, y la caja tiene que poder correr en el motor más simple que exista.

## Lo que este repositorio NO hace

- **No imputa el abono a la deuda.** Eso es `rentas` (ADR-0026 §2): aquí se cobra contra una orden y
  se publica el pago; qué parte de la deuda extingue lo decide el libro. Si la caja imputara, la
  regla del art. 31 del Código Tributario estaría escrita dos veces.
- **No conoce tributos, ni fases, ni conceptos.** `OrdenDeCobro` no tiene un campo `ejercicio` ni
  `tributo`, y `PeticionDeOrdenDeCobro` tampoco. **Ésa es la definición práctica de la frontera**: el
  día que uno de los dos gane ese campo, la caja habrá dejado de servir para cobrar un puesto de
  mercado.
- **No le pregunta nada a nadie para cobrar.** `kamayuk-caja-nucleo` no declara
  `implementation(project(...))` de ningún otro contexto y `CajaController` no inyecta un solo puerto
  hacia otro sistema. Es lo que hace cierto que la ventanilla cobre con `rentas` apagado.
- **No decide D-17 ni D-20.** Ver abajo.
- **No decide la etiqueta de su imagen, ni su namespace, ni sus `PriorityClass`.** Las pone `infrastructure`.
- **No tiene `git log` de su historia.** La tiene `sgtm`, que no se borra.

## Estructura

```
backend/                        Gradle. Java 25, Spring Boot 4
  kamayuk-caja-dominio-compartido/  objetos de valor y contexto de tenant
  kamayuk-caja-esquema/             el baseline, V2, el migrador y la prueba de aislamiento
  kamayuk-caja-plataforma/          del token al SET LOCAL, auditoría, documentos, borde HTTP
  kamayuk-caja-nucleo/              EL contexto acotado. Se llamaba `caja` (R-N)
  kamayuk-caja-aplicacion/          ensambla y aloja las barreras
frontend/                       caja-web: la interfaz de ventanilla. React 19 + Vite, sin backend
  src/                              lo que se despliega. Es la ruta que vigila la guarda del registro
    ds/                               los tokens de V6 y los estilos globales (#4)
    datos/                            todo lo que las pantallas ensenan, copiado del artboard (#5)
    barra/                            la barra global, el aviso de servicio y el toast (#6)
    arbol/                            el arbol de modulos y su filtro (#7)
    marco/                            pestanas, hash, titulo y estados vacios (#8)
    paleta/                           la paleta de comandos y sus diez acciones (#9)
    pantallas/                        las cuatro propias: el Panel (#10), la lista de Recibos (#11),
                                      la ficha y sus campos (#12), el cobro nuevo (#13) y, con #14,
                                      Cajas y arqueo, Tarifario y cierre y su tabla compartida
  verificaciones/                   las pruebas de Vitest, las muestras de ESLint y los cuatro
                                    arneses de navegador: paleta, pegajosa, mirar y cero-red
infrastructure/                 el descriptor de despliegue en TypeScript, con yarn
despliegue/                     compose.yaml: los tres procesos del backend y `caja-interfaz` (#18)
docs/                           los ADR que enlaza, hallazgos de RLS, P5D y la guía de desarrollo
  00-gobierno/                    P5D, la guarda del registro y los huecos que quedan en
                                  `infrastructure` (#18)
```

El backend **no compila sin `infrastructure` clonado al lado**: las barreras se consumen como
*composite build* desde `../../infrastructure/librerias-backend`. `settings.gradle.kts` lo
comprueba antes y falla diciendo qué `git clone` falta.

Los paquetes son `kamayuk.caja.*` y el contexto acotado es `kamayuk.caja.nucleo.*`. **Se llamaba
`kamayuk.caja.caja` hasta R-N (2026-09-05)**: el patrón `kamayuk-<sistema>-<contexto>` produce el
nombre repetido allí donde el sistema tiene un solo contexto y se llama igual que él —pasaba en
`caja`, en `catastro` y en `rentas`—, y la dirección pidió quitarlo. El contexto pasa a llamarse
`nucleo` y el patrón queda intacto; `normativa` no cambia porque su contexto ya se llama
`parametros`. El porqué y lo que costó están en
`infrastructure/docs/00-gobierno/R-N-los-tres-modulos-repetidos.md`. Los **roles de base de datos son
`kamayuk_owner`, `kamayuk_app` y `kamayuk_readonly`** (etapa C del renombrado): son del **clúster**,
que los cuatro sistemas comparten, así que se renombran en los cuatro a la vez o en ninguno.

## Las dos piezas que hay que entender antes de tocar nada

**La orden de cobro.** Es lo único que esta caja sabe cobrar: de dónde viene, cómo la llama quien la
mandó, qué dice el papel, cuánto, desde cuándo y **a qué fecha está esa cifra**. La caja no
recalcula: imprime lo que le dieron. Y su `referenciaExterna` es **opaca** — no se analiza, no se
compara por partes, no se ordena—, que es lo que permite que el día de mañana sea el contrato de un
puesto de mercado.

**El buzón de salida.** Se escribe **en la misma transacción que el recibo**. Si la fila está, el
recibo está. Un proceso aparte lo entrega y lo marca. Lo que compra es que la ventanilla cobre con
el sistema de origen apagado; lo que cuesta es que **la conciliación diaria deje de ser buena
práctica**. El `pagoId` lo genera **la caja** al cobrar, no el transporte: un reintento manda el
mismo, y por eso el receptor puede deduplicar.

## Antes de escribir código, leer

| Si vas a tocar… | Lee |
|---|---|
| Cualquier cosa | [ADR-0002 — Estrategia multi-tenant](https://github.com/hneyra/infrastructure/blob/main/docs/30-arquitectura/adr/ADR-0002-estrategia-multi-tenant.md) — es el riesgo número uno |
| Base de datos | [Los cinco hallazgos de RLS](docs/40-datos/hallazgos-de-rls.md) **primero** |
| El camino del dinero | [ADR-0026](https://github.com/hneyra/rentas/blob/main/docs/30-arquitectura/adr/ADR-0026-el-camino-del-dinero.md) — dos transacciones, un *outbox*, y la imputación en `rentas` |
| El contexto de municipalidad | [ADR-0028](https://github.com/hneyra/infrastructure/blob/main/docs/30-arquitectura/adr/ADR-0028-el-tenant-no-cruza-por-http.md) — token delegado, jamás una cabecera |
| Backend | [ARQ-04 — Estándares de código](https://github.com/hneyra/infrastructure/blob/main/docs/30-arquitectura/estandares-de-codigo-backend.md) |
| Montar el entorno | [D0 — Desarrollo](docs/D0-desarrollo/README.md) |

Índice de decisiones: [`docs/30-arquitectura/adr/README.md`](docs/30-arquitectura/adr/README.md).

## Decisiones abiertas que bloquean

Registro completo en [GOB-02](https://github.com/hneyra/sgtm/blob/migracion-a-microservicios/docs/00-gobierno/decisiones-abiertas.md).

| # | Decisión | Bloquea |
|---|---|---|
| D-17 | **El padrón de administrados cuando la caja cobre lo que no es tributo** | Su primer ADR, y su frontera con `rentas` |
| D-20 | **Qué dice el recibo cuando la imputación es asíncrona** | El papel que se entrega en ventanilla |
| D-14 | Regla de imputación de un pago parcial | La conciliación con el libro |
| D-03d | Redondeo del importe a pagar en el cierre, que puede no ser el del cálculo | El arqueo |
## Reglas que no se negocian

Son las mismas en los cinco repositorios, y las verifica **el mismo artefacto**:
[`comun-verificaciones`](https://github.com/hneyra/infrastructure/tree/main/librerias-backend/comun-verificaciones),
que vive en `infrastructure` y se consume como *composite build*.

| # | Regla | Motivo |
|---|---|---|
| 1 | **Importes en `BigDecimal`/`NUMERIC`.** Prohibidos `double` y `float` | Precisión monetaria (RNF-055) |
| 2 | **Ningún método de dominio recibe `municipalidadId`.** Sale del token, se fija una vez con `SET LOCAL` | Si el desarrollador no lo maneja, no puede olvidarlo |
| 3 | **`SET LOCAL`, jamás `SET SESSION`** | `SET SESSION` sobrevive al retorno de la conexión al pool y contamina la petición de otra municipalidad |
| 4 | **Sin `DELETE`** en deuda, pagos, recibos, valores, valuaciones, asientos ni auditoría. Se anula, se da de baja o se reversa | RNF-051, y el manual §Auditoría |
| 5 | **Ningún literal numérico tributario en el código.** UIT, tramos, alícuotas, valores unitarios, aranceles y tablas de depreciación viven en datos versionados | Reproducibilidad y cambio sin despliegue (RNF-053) |
| 6 | **Las reglas tributarias son funciones puras.** Sin base de datos, sin reloj, sin configuración global; la fecha entra como argumento | Recalcular 2027 en 2037 debe dar el mismo céntimo |
| 7 | **Nada de Spring ni JPA en la capa `dominio`** | Las reglas deben probarse sin levantar el contexto |
| 8 | **`alicuota`, nunca `tasa`**, para un porcentaje | `tasa` es un tipo de tributo |
| 9 | **No existe «la deuda»:** es `deudaActualizadaA(fecha)`, y toda cifra mostrada indica su fecha | RNF-075 |
| 10 | **Toda modificación de datos exige observación del usuario.** Sin observación no se guarda | Manual §Auditoría; RNF-052 |

Las reglas 1, 2, 6, 7 y las fechas están escritas como pruebas de ArchUnit; `SET SESSION` y
`DELETE` sobre tabla protegida, como escáner del código fuente. Se añade una **undécima**, que
sólo existe desde que hay cinco repositorios: **ningún SQL cruza la frontera de sistema** —un
`JOIN` contra una tabla de otro sistema no deja huella en el bytecode, así que la vigila un
escáner de texto y no ArchUnit—.

> **Y hasta P5E esa undécima regla estaba MUDA aquí, medido.** `ConfiguracionDeCaja` sólo repartía
> las tablas de este esquema, y el escáner distingue a propósito tres casos —lo propio, lo replicado
> y **lo que nadie repartió**—, de los que el tercero **no es un cruce**: uno que marcara toda tabla
> desconocida gritaría en cada archivo y dejaría de leerse (#437). De modo que un
> `SELECT … FROM contribuyente JOIN predio` en `src/main` pasaba en **verde**.
>
> El reparto nombra ahora también las 88 tablas de `rentas`, las 15 de `catastro` y las 6 de
> `normativa`, como ya hacían esos dos repositorios. No están en esta base y **justamente por eso
> hay que nombrarlas**. En cuanto pudo ver, la regla encontró un cruce vivo: `ReciboRepositoryJdbc`
> resolvía el filtro del listado con una subconsulta a `contribuyente`, o sea la mitad de
> `PENDIENTE-CRUCE-06` que P5D no cerró —cerró la **emisión** copiando el pagador, no la
> **búsqueda**—. Hoy el listado se filtra por `?documento=`, contra `recibo.pagador_documento` y su
> índice. Todo en [P5E §2](https://github.com/hneyra/rentas/blob/main/docs/00-gobierno/P5E-cierre.md).

**Si agregas una regla, agrega también la clase de muestra que la viola**, en las `muestras/` de
`comun-verificaciones`: una regla que no puede fallar no protege nada. Y lo exige por
construcción `ReglasDeArquitecturaMuerdenTest`, un `@TestFactory` sobre todas las reglas: una
regla sin muestra sale roja sola.

Lista completa con su justificación:
[ARQ-04 — Estándares de código del backend](https://github.com/hneyra/infrastructure/blob/main/docs/30-arquitectura/estandares-de-codigo-backend.md).

## Idioma

Español en el dominio, inglés en lo técnico. **Sin tildes en identificadores**: Checkstyle lo
revisa en el backend, ESLint en el descriptor.

```java
public final class Papeleta { … }                  // dominio: español
public interface PapeletaRepository { … }          // patrón: inglés
autovaluo.calcularTotal();                         // comportamiento: español
repository.findById(id);                           // infraestructura: inglés
```

Tablas y columnas en español `snake_case`. Campos de la API JSON en español `camelCase`.
Comentarios, pruebas y mensajes de commit en español.
## Comandos

```bash
cd backend
./gradlew verificarArquitectura   # ArchUnit, escaner de fuentes, aserciones y frontera de sistema
./gradlew verificarArranque       # el artefacto levanta en los dos perfiles (C-7). Requiere PostgreSQL 16
./gradlew verificarAislamiento    # aislamiento multi-tenant. BLOQUEANTE. Requiere PostgreSQL 16
./gradlew build                   # lo anterior mas Spotless
./gradlew spotlessApply           # arregla el formato en vez de solo reprocharlo

cd ../infrastructure
yarn install && yarn verificar    # el descriptor: lint, tipos y pruebas. Sin Pulumi ni cluster

cd ../frontend
yarn install && yarn verificar    # caja-web: ESLint (con sus muestras), tipos y Vitest
yarn build                        # el artefacto de produccion, en frontend/dist/
yarn dev                          # el servidor de desarrollo. No necesita backend: no habla con nadie

# Los cuatro arneses de navegador. Necesitan la aplicacion servida y el Chromium de Playwright
# (`npx playwright-core install chromium`); `CAJA_BASE` dice donde, por omision el de `yarn dev`.
yarn paleta                       # la paleta de comandos, solo con el teclado (#9)
yarn pegajosa                     # la cabecera de las tablas se queda al desplazar (#14)
yarn mirar                        # las cuatro secciones, los cortes, el teclado y el papel (#15)
yarn cero-red                     # ni una peticion fuera de los recursos propios (#15)

# La plataforma: PostgreSQL con las cuatro bases, Keycloak con sus dos realms, Traefik y el buzon
cd ../../infrastructure
docker compose -f despliegue/plataforma.compose.yaml up -d --wait

# Lo de ESTE sistema contra ella: sus tres procesos y su interfaz (#18). El `.env` es el de
# la plataforma, y la red `kamayuk-plataforma` tiene que existir ya (`external: true`).
# `caja-interfaz` se levanta SOLO: no arrastra migrador, implantacion ni backend.
cd ../caja
docker compose -f despliegue/compose.yaml up -d --build --wait
docker compose -f despliegue/compose.yaml up -d --build caja-interfaz --wait
curl -sf http://localhost:${KAMAYUK_PUERTO_INTERFAZ_CAJA:-8082}/

# La guarda del registro (#711) y su autoprueba
node docs/00-gobierno/verificar-fila-del-registro.mjs
node docs/00-gobierno/verificar-las-muestras-del-registro.mjs
```

**`verificarAislamiento` no se omite sin Docker: falla.** Una prueba bloqueante que se salta a sí
misma deja el build en verde sin haber verificado nada. La salida documentada es apuntar a un
PostgreSQL 16 que ya exista, y **ninguna que omita la prueba**:

```bash
./gradlew verificarAislamiento \
  -Dkamayuk.pruebas.postgres.url=jdbc:postgresql://localhost:5432/postgres \
  -Dkamayuk.pruebas.postgres.usuario=postgres \
  -Dkamayuk.pruebas.postgres.clave=…
```

Tiene que ser **PostgreSQL 16** —el esquema no corre en 18 (`V11` falla con «text search
dictionary "unaccent" does not exist»)— y superusuario, porque la prueba crea los cuatro roles.
Cómo montarlo desde cero: [D0 — Desarrollo](docs/D0-desarrollo/README.md).
## Verificar antes de afirmar

**Ejecutar la prueba vale más que razonar sobre ella.** Y no basta con que la verificación esté
escrita: **tiene que demostrarse que puede fallar** — se rompe a propósito el código que protege,
se ejecuta, y se anota el rojo exacto que sale.

Cada issue deja aquí una fila con qué se implementó, **con qué rotura se demostró que la
verificación muerde** y qué rojo produjo. Es lo que impide volver a descubrir el mismo hallazgo
por tercera vez.

> **La tabla nace vacía, y es correcto que se vea así.** El registro anterior —288 filas, issue a
> issue— es historia de `sgtm` y **no viaja**: en un repositorio sin ese `git log` sería el
> registro de un trabajo que aquí no se hizo. Vive en
> [`sgtm/CLAUDE.md`](https://github.com/hneyra/sgtm/blob/migracion-a-microservicios/CLAUDE.md),
> que no se borra. Se consulta; no se copia.

Que la fila **exista** lo comprueba `docs/00-gobierno/verificar-fila-del-registro.mjs` en cada PR
que cierre un issue y toque código de producción. Lo que la fila **diga** —que la mutación sea
real y las cifras cuadren— no lo puede leer una máquina: eso lo lee la revisión.

| Verificación | Cómo se demostró que puede fallar | Resultado |
|---|---|---|
| El baseline aplica sobre una base **sin ninguna extensión** (P5D) | Ejecutando el baseline **original** de `sgtm/docs/40-datos/baselines/caja/` sobre la misma base | **Muere en su línea 204**: «ERROR: text search dictionary "unaccent" does not exist», dentro de `nombre_normalizado(text)` — una función de `catastro` y `rentas` que el generador arrastró y que **ninguna columna de esta caja usa**, porque aquí no hay una sola columna generada. Con las ocho funciones ajenas y los cinco dominios muertos fuera, `V1` aplica entera: 23 tablas, `0 extensiones no-plpgsql`. Y eso permitió recortar `crear-roles.sql` a **tres roles y cero extensiones** — que es lo único que hace creíble «con `rentas` apagado la ventanilla sigue cobrando»: una ventanilla cuya base necesita PostGIS no se levanta en cualquier sitio |
| El `REVOKE UPDATE ON cierre_caja` que `V32` del monolito no pudo hacer (P5D, `V2`) | Reproduciéndolo contra PostgreSQL 16.15 real, y midiendo después la alternativa | Con el `REVOKE` puesto, `SELECT … FOR UPDATE` da **«permission denied for table cierre_caja»** — el hallazgo de `V32` §1.bis, reproducido tal cual. **Y la premisa de ADR-0026 es falsa**: dice que se replantea «donde el cierre ya no comparte base con el libro», y que el libro estuviera al lado nunca tuvo nada que ver. Lo que impedía el REVOKE es que la ventanilla se serializaba **ahí**; movida a `orden_de_cobro`, el turno se abre con `INSERT … ON CONFLICT DO NOTHING`, que **no necesita el privilegio** —medido: dos ejecuciones seguidas dejan un solo turno con `has_table_privilege(…,'UPDATE')` en `f`— y el REVOKE se puede hacer |
| Con el sistema de origen apagado, la ventanilla **sigue cobrando** (P5D AC 2) | Apuntando el cliente HTTP a **un puerto que nadie escucha** —`ServerSocket(0)` abierto y cerrado—, no a un doble que lanza | Cobra, emite recibo, marca la orden `PAGADA` y deja el pago `PENDIENTE` **con su hora**; el publicador reintenta sin perderlo y, agotados los intentos, **muere y dispara la alerta**. Y la conciliación de ese día **no trae ceros**: trae su motivo. Un cero se leería como «no aplicaron nada» —indistinguible de un día sin cobros— y la conciliación diría que cuadra, que es el criterio de #48 con la licencia de «valor de obra 0,00» |
| La conciliación del día cuadra, **y deja de cuadrar** (P5D AC 1, parcial) | Tres roturas sobre ocho días simulados con un sistema de origen levantado de verdad | El origen aplicando 40,00 donde se cobraron 100,00 → `diferencia = 60,00`; **el origen rechazando uno con la cifra coincidiendo → tampoco cuadra**, porque cuadrar con un pago sin imputar es cuadrar por casualidad; y un evento devuelto a `PENDIENTE` —que es como está entre los dos `COMMIT`— tampoco. **Los treinta días del criterio NO se cumplen y no se pueden cumplir aquí**: son tiempo de calendario. Está declarado en P5D §2 |
| Un pago inyectado dos veces produce **un solo asiento** (P5D AC 3, en `rentas`) | Diez hilos de verdad entregando el mismo `pagoId` a la vez | **1 asiento y 1 fila de buzón.** La garantía es `pago_recibido_uq` sobre el `pagoId` **que generó la caja**, no un `if`. Y escribir la prueba encontró **dos defectos vivos**: `jsonb` no devuelve el texto que se guardó —lo reserializa con un espacio detrás de los dos puntos, así que la referencia de la orden no se encontraba y el pago se rechazaba diciendo «no trae ninguna obligación» sobre un cuerpo que las trae todas (#653 otra vez)—; y **atrapar la excepción del libro no sirve de nada**, porque sale de un `@Transactional` anidado y deja la de fuera *rollback-only*. La primera corrección —marcar el rechazo en transacción nueva— **siguió fallando**: la fila que iba a marcar tampoco estaba. Por eso el rechazo **se inserta y no se marca** |
| Una anulación **reversa y no borra** (P5D AC 4) | Contando filas del libro antes y después, y con el escáner de fuentes | Tras la anulación los asientos del cobro **siguen ahí** y el libro tiene **más filas que antes**, marcadas con `ANULACION RECIBO …`. Una anulación inyectada dos veces **no reversa dos veces** —lo haría dejar al contribuyente debiendo el doble de lo que pagó (#34)—. Y que no borra **no lo dice una lectura del código**: `cuenta_corriente_asiento` está en `TABLAS_PROTEGIDAS` de `rentas`, y en `caja` la lista se reescribió para este sistema con **siete** tablas, incluidas `orden_de_cobro` y `pago_evento` |
| El listado de recibos, con al menos una fila (P5D) | **No hubo que provocarla: la escribió el agente que adaptó las pruebas y salió roja.** Lo que sí se midió es qué la esconde | `ReciboRepositoryJdbc.buscar` seleccionaba `contribuyente_id` y **no** `pagador_documento` ni `pagador_nombre`, que es lo que su mapeador lee desde P5D: toda página con una fila reventaba con «Der Spaltenname pagador_documento wurde in diesem ResultSet nicht gefunden». **Y una página vacía no lo destapa** —el mapeador no llega a correr—, así que `unaBusquedaSinResultadosDaPaginaVacia` pasaba en verde con el listado entero roto: la prueba más barata de escribir es justo la que no puede verlo. De paso salió un segundo defecto: `pagadorDe` leía `wasNull()` **de la columna equivocada** —los argumentos se evalúan de izquierda a derecha—, así que un recibo con documento y sin nombre perdía su `idExterno`, que es el dato con el que el evento le dice al origen a quién imputar |
| El interbloqueo del rechazo de un pago (P5D, en `rentas`) | **No hubo que provocarlo: la corrida se colgó.** Y lo que lo dijo no fue ninguna prueba | `pg_stat_activity` con la primera conexión en `idle in transaction` y la segunda en `Lock / transactionid` sobre el `INSERT INTO pago_recibido`. Llamar a `RecibirPago.recibir` **dentro** de una transacción deja a la de fuera abierta con la fila del buzón insertada y marcada *rollback-only*, y el rechazo abre una nueva que intenta insertar el mismo `pago_id`: el índice único la hace esperar a la primera, y la primera espera a la segunda. **Un cuelgue sin mensaje en el camino del dinero**, que es lo peor que puede pasar. La guarda vive ahora en el código y dice qué hacer; en producción no puede ocurrir porque `PagoController.recibir` no lleva `@Transactional`, y ahora tampoco puede empezar a ocurrir sin que alguien lo vea |
| La regla 10 sobre el publicador del buzón | **Tampoco hubo que provocarla: ArchUnit la encontró.** P5D había afirmado por escrito que la lista de exenciones quedaría vacía | Rojo nombrando `EntregarEventos.entregarUno`. La regla mira la **firma** de un método transaccional que escribe, no la naturaleza de lo que escribe — y ésa es la propiedad que la hace útil: «esto no es un dato de verdad» es lo que cualquiera puede escribir sobre cualquier escritura. Entra en la lista con su motivo real (no hay ningún usuario delante), y al lado queda `ExplicarPagoSinEntregar`, que **sí** exige observación porque ahí hay alguien decidiendo que ese dinero no se registra |
| El recuento de pruebas, contado una a una (P5D) | **Contando los métodos anotados de cada clase borrada contra los de su heredera**, no comparando totales | 3 246 − 191 (las 14 clases que salieron de `rentas`) + 5 (`PagoInyectadoDosVecesTest`) = **3 060**, que es lo que la corrida mide. Y el conteo destapó **dos cosas que un total nunca habría enseñado**: `AltaDeCajasJdbcTest` medía **10 donde el original decía 11** —el método que lee el archivo real `ejemplos/cajas.csv` se había quedado atrás porque el archivo estaba en `rentas` y el importador viajó aquí; se trajo el archivo con su README y volvió—, y **siete métodos de `CobrarEnVentanillaTest` no tenían contraparte en ningún repositorio**, los siete de la mitad de deuda. Ésos no eran un descuido ni un hueco: eran una **regresión que la propia extracción introdujo** — medían `CobrarDeuda`, que leía el libro y cobraba en un solo acto, y de sus dos mitades **sólo se había reescrito la de cobrar**, de modo que la ventanilla podía cobrar una tasa de punta a punta y una deuda tributaria **no**, porque `OrdenesDeCobro` era un puerto con adaptador HTTP y **cero invocadores**. Se cerró escribiendo la mitad que faltaba (`EmitirOrdenDeCobro` y `POST /rentas/api/v1/ordenes-de-cobro`, en `rentas`) y reponiendo la cobertura: cinco métodos volvieron reescritos contra el camino nuevo, dos se retiraron **con su motivo** —uno porque lo que medía lo mide hoy `PagoInyectadoDosVecesTest` contra PostgreSQL real, otro porque `TipoDePago` ya no cruza esta frontera—, y con ellos entraron nueve afirmaciones que el camino viejo no podía tener. **Ninguna prueba se quedó sin sitio, y el conteo es lo único que lo delató** |
| **C-6 — `cargar-cajas.sh` vive donde vive `CargarCajas`** (el paso 4 de la siembra, hueco 11 de P5D) | Del lado de `infrastructure`, devolver el guion a donde estaba y medir el censo que cruza cada guion con los `@ConditionalOnProperty` de su repositorio | 2 en rojo en `siembra-de-la-demostracion.test.ts`. **El guion estaba en `infrastructure` y el cargador aqui**, y un guion lanzado contra la imagen equivocada arranca la aplicacion, no carga nada y sale con codigo 0 — medido con `cargar-transferencias-demo.sh`, que estaba en `catastro` y su proceso en `rentas`. Sembrado de verdad contra PostgreSQL 16.15: **5 ventanillas y 3 areas**, y esas dos cifras no estan escritas en ninguna parte —salen de `cajas.csv`, sus filas y sus `codigoArea` distintos—. Sigue sin exigir `es_demostracion`, que es lo correcto: una ventanilla no es un dato inventado (#430). Las 673 pruebas siguen en 673, 0 fallos, con `--rerun-tasks` contra el motor real. **Y ejecutar destapo que esta aplicacion no arranca**: no tiene ninguna implementacion de `ComprobadorDeAcceso` y le falta el `ObjectMapper` de Jackson 2 que inyecta `ComponedorDeEventosJson`; declarado, no arreglado, en [C-6](https://github.com/hneyra/infrastructure/blob/main/docs/00-gobierno/C-6-la-siembra-orquestada.md) §6 |
| **C-7 — `caja` arranca por primera vez** ([C-7](https://github.com/hneyra/infrastructure/blob/main/docs/00-gobierno/C-7-que-arranquen.md): el módulo `kamayuk-caja-seguridad`, las dos variables de ADR-0026 §4 en el descriptor, la prueba de arranque y `verificarArranque`) | Tres roturas, cada una sola y restaurada por copia comparada con `cmp`: quitarle el `@Component` a `ComprobadorDeAccesoJdbc`; quitar del descriptor las dos variables de conciliación; y volver la precedencia del comprobador una unión | **4 de 4** la primera —«required a bean of type `kamayuk.caja.autorizacion.ComprobadorDeAcceso`»—; 2 la segunda, en `infrastructure`, nombrando las dos variables y el pod que no levanta; 2 la tercera. **`caja` no se había arrancado nunca**, y este trabajo es donde se ve por qué importaba: además del comprobador que faltaba, su `application.yaml` exige `KAMAYUK_CAJA_RESPONSABLE` y `KAMAYUK_CAJA_CANAL` **sin valor por omisión** —ADR-0026 §4 pide una alerta a una persona con nombre—, y `EntornoDelDescriptor` no tenía campo para ellas: el hueco estaba escrito en el propio descriptor de `caja` y no se podía cerrar desde aquí. **Y el cuerpo de los eventos de pago no cambió un byte** al pasar `ComponedorDeEventosJson` a Jackson 3: se midió serializando el mismo árbol con las dos versiones, y `ContratoQueConsumeDeRentas` sigue en verde. La implantación se ejecutó contra una base creada de cero: «Municipalidad 200105 lista en caja (DEMOSTRACION): id 1, 3 accesos nuevos» |
| **D — quien publica las dos imagenes de `caja`** (`publicar-imagenes.yml`: `kamayuk-caja` y `kamayuk-caja-migrador`, etiquetadas con el `sha` de este repositorio, mas el trabajo que le pregunta al registro si la etiqueta se puede pedir) | La rotura no hubo que provocarla: **el estado de partida era el defecto**. Medido contra `ghcr.io` el 2026-09-05 con un token emitido por `https://ghcr.io/token`, las dos etiquetas que el manifiesto de `infrastructure` pide contestaban `404 MANIFEST_UNKNOWN` | Ninguno de los cinco repositorios publicaba una sola imagen —`publicar-imagenes.yml` se quedo en `sgtm`, el archivo historico, y lo que los cinco tienen se llama `registro.yml` y es la guarda de #711—, asi que un `pulumi up` habria dejado los pods en `ImagePullBackOff` **sin que nada lo predijera**: el manifiesto es valido y el planificador ubica el pod. **Dos decisiones con su motivo.** (1) La etiqueta es el `sha` de ESTE repositorio y no `applicationBootstrapVersion` —que es un `sha` de `sgtm`, una revision que ni siquiera existe en este clon—: una etiqueta que no resuelve contra ningun `git log` no identifica nada, y entonces «que corre en la municipalidad» deja de tener respuesta. (2) **Sin filtro `paths`**, al reves que el flujo del monolito, para que valga la equivalencia que la guarda de `infrastructure` necesita: *todo commit de `main` tiene sus dos imagenes*. Con filtro, un merge de solo documentacion deja un `sha` de `main` sin imagenes y «esta en la historia de main» deja de implicar «se puede desplegar», en silencio. **Y el trabajo `comprobar` no sobra**: un `build-push-action` en verde dice que el `push` no dio error; que la etiqueta se pueda PEDIR es otra afirmacion, y es la que decide si el pod arranca. Distingue los tres desenlaces a proposito, porque el tercero engaña: `200` existe, `404` no existe, y `403 DENIED` —lo que recibe un PAT de escritorio sin `read:packages`, comprobado— **no permite concluir nada** y por eso tambien falla, en vez de dar por buena cualquier respuesta que no sea 404 |
| **T-0 — `frente_predio` entra en el reparto de tablas, aunque este sistema no la tenga** (la undecima regla: ningun SQL cruza la frontera de sistema) | La medida es la de R-N, y no hizo falta repetirla: el reparto se consulta con `getOrDefault(tabla, SISTEMA_REPLICADO)`, y «replicado» significa «no esta a ningun lado de la frontera» | Una tabla que **falta** en el mapa no pone nada rojo: **deja de revisarse**, en verde. Por eso la tabla nueva de `catastro` se nombra aqui el mismo dia que nace y no el dia que alguien la consulte por error — que es el dia en que ya seria tarde. Nombrar de mas una tabla que este sistema no tiene **no cuesta nada** (ningun archivo suyo la menciona) y es lo que hace que el cruce, si llega, se vea. `./gradlew build` en verde **Cifras, con la linea base medida en el mismo entorno**: `catastro` **999 -> 1 011**, `rentas` **3 150 -> 3 161**, `normativa` **623 -> 634** y `caja` **693 -> 704**, 0 fallos los cuatro contra PostgreSQL 16.13 + PostGIS 3.4.2 real. Los **+11** son los mismos en los cuatro y salen de la libreria compartida —nueve pruebas nuevas del escaner mas las dos reglas de ArchUnit, que `ReglasDeArquitecturaMuerdenTest` cuenta una por regla—; el **+12** de `catastro` es esa docena mas el caso del marco en la prueba de aislamiento. `yarn verificar` no se mueve: 38 rojas antes y 38 despues, las mismas una a una. |
| **#3 — el andamio de `caja-web`** (`frontend/`: React 19 + Vite 6 en un solo paquete, `yarn verificar` = ESLint con **tres reglas propias** + `tsc` + Vitest, `yarn build`, el escudo y `.github/workflows/frontend.yml`) | Dos roturas, cada una sola y restaurada por copia comparada con `cmp`: (1) quitarle a la muestra `identificador-con-tilde.ts` la infracción —`alícuota` → `alicuota`, `añoDeEmision` → `anioDeEmision`—; (2) quitarle a `eslint.config.mjs` la **regla**, que es lo que de verdad se protege | **1 de 8 en rojo** la primera: «Se esperaba un mensaje que casara con /Sin tildes ni eñe en identificadores/. Se obtuvo: (ninguno)», nombrando la prohibición por su nombre. La segunda **no llegó a la prueba**: `yarn verificar` encadena lint antes que test y `eslint .` se puso rojo primero con «'LETRAS_ACENTUADAS' is assigned a value but never used», o sea que una regla borrada a medias la caza el lint sin que la prueba tenga que opinar; borrando también la constante sale el mismo 1 de 8. **Y ejecutar destapó un defecto que ni el lint ni las pruebas podían ver**: `vitest@3.2.7` colgó su propio `vite@7` bajo `node_modules/vitest/`, y con dos copias de los tipos de Vite el `typecheck` murió en `vite.config.ts` con «TS2769 … Type 'Plugin<any>[]' is not assignable to type 'PluginOption'»; se fija con `resolutions` a `vite@^6.0.11` —el `tsc` de la cadena es lo único que mira la coherencia de las tres etapas—. Medido además con un navegador de verdad, no con jsdom: `yarn dev` (5181) y `vite preview` sobre `dist/` (5182) dan los dos `<h1>caja-web</h1>`, título «Tesorería · caja-web», el escudo con tamaño natural **350×420** y **cero errores de consola**; `eslint . --format json` linta **7 archivos** e ignora las **3 muestras**, que es la propiedad que hace que el andamio no nazca en rojo por su propio material de prueba |
| **#4 — los tokens de V6** (`frontend/src/ds/`: `tokens/{colores,tipografia,formas}.css` como *custom properties*, `global.css` con el bloque `<style>` del artboard portado entero, las cuatro insignias en `ds/tokens.ts` y la carga de Source Sans 3 en `index.html`) | Dos roturas, cada una sola y restaurada por copia comparada con `cmp`: (1) `--azul: #005284` → `#005285`, la que pide el issue; (2) borrar de `global.css` la **regla** `input:focus …`, que es lo que de verdad se protege | **3 en rojo** la primera, desde tres ángulos que no se solapan: la tabla de tokens («expected '#005285' to be '#005284'»), el inventario que exige que **ningún** hexadecimal declarado en `tokens/` esté fuera de la lista —lo que caza un color *nuevo*, cosa que la tabla no puede— y el `borderColor` de un `<input>` **enfocado de verdad**, que dice que el token no sólo está declarado sino que llega al elemento. **3 en rojo** la segunda: «expected '' to be '0 0 0 3px #D3EBFA'». **Pero lo que este issue deja de verdad es una afirmación falsa que estuvo escrita aquí y hubo que retirar.** La primera versión sostenía que «el `getComputedStyle` de jsdom ignora toda regla con pseudo-clase». **Es falso**: con la regla en hexadecimal literal, jsdom devuelve el `box-shadow` entero. Lo que jsdom **no** hace es resolver `var()` —le sale `0 0 0 3px var(--anillo-campo)`—, y como este diseño está escrito con tokens, ésa es toda la diferencia que justifica happy-dom: el motivo era otro, no el que se había escrito. Lo que engañó fue el **orden de la medición**: el entorno memoriza el estilo calculado de cada elemento y `focus()` no invalida esa memoria, así que la sonda leyó antes de enfocar, vio `""` y se lo achacó a la pseudo-clase. **La memorización tampoco es de happy-dom: jsdom hace lo mismo**, medido, y estaba mal atribuida. **Y había algo peor que la redacción**: la aserción fuerte pasaba **por el orden en que caían las pruebas**. Dentro de `tokens.test.ts`, leer el estilo de un input sin foco envenenaba la sustitución de `var()` para todo lo que viniera después —comprobado por eliminación: quitando esa prueba, la aserción volvía a pasar—, de modo que el campo enfocado sólo salía bien porque corría antes. La corrección no es un `beforeAll` sino **separar las pruebas de foco a `foco.test.ts`**, porque Vitest aísla el entorno por archivo y allí el efecto desaparece; y va acompañada de una prueba que **afirma esa independencia del orden**, para que si algún día vuelven a compartir archivo salga roja ahí y no en una pantalla. Verificado además con `--sequence.shuffle`, tres corridas, 75 de 75. Con ello se fue una prueba que **no podía fallar por el motivo que decía comprobar** —afirmaba que `navigator.userAgent` contenía «HappyDOM»—, sustituida por dos que miden sobre un jsdom levantado en el propio archivo: que `:focus` **sí** aplica y que `var()` **no** se resuelve. El día que jsdom lo resuelva, salen rojas solas y la excepción sobra. **Y queda dicho lo que esto NO prueba**: lo que un navegador *pinta*. Eso es el arnés de Playwright del issue de accesibilidad; esto es lo más fuerte que un emulador de DOM puede afirmar. **El criterio 2 del issue está mal planteado y ya salía rojo sobre `main` antes de este PR**: su `grep -ri` busca «Inter» como subcadena y «interfaz» la contiene, así que casa con un comentario de #3 que no tiene nada de la paleta descartada. Se comprueba de verdad con `\bInter\b`, que deja `frontend/src` limpio. La contorsión que se había hecho para esquivarlo —declarar `ColoresDeInsignia` con `type` en vez de `interface`— **se revirtió**: deformar el código para que pase una comprobación mal escrita es peor que la comprobación. **Un último modo de fallo, visto al pasar**: con dos archivos de prueba que no cargaban, Vitest imprimió «`Test Files 2 passed (4)`» — «2 de 4» y aun así la palabra *passed*. El recuento entre paréntesis es el que hay que leer. Las pruebas quedan en **75** y `yarn build` emite por primera vez una hoja de estilos: `dist/assets/index-*.css`, **2,59 kB** (gzip 1,16), con la cadena de tres `@import` ya aplanada. |
| **#5 — los datos de proxy y los vocabularios** (`frontend/src/datos/`: `tabla.ts`, `navegacion.ts`, `cajas.ts`, `recibo.ts`, `recibos-del-turno.ts`, `arqueo.ts` y `tarifario.ts`, copiados de las líneas 942-1215 de `TesoreriaV6.dc.html`, más el escáner de importes de `src/` y **21 pruebas nuevas**) | Cuatro roturas, cada una sola y restaurada por copia comparada con `cmp`: (A) la que pide el criterio 4, `deudaTotal: 3455.24`, **con su campo declarado `number`**; (B) la misma cifra **sin declarar el campo**, que es el descuido de verdad; (C) quitar el `eslint-disable` de la única excepción; (D) transponer dos dígitos de una celda, `2,055.04` → `2,055.40` | **(A)** `yarn lint` en rojo —`src/datos/recibos-del-turno.ts:82:25`, «Un importe es texto y jamás number: como number pierde céntimos»—, `tsc` **verde**, y el escáner rojo nombrando **las dos formas**: `:82 deudaTotal (declaracion)` y `:98 deudaTotal (literal)`. **(B) `yarn lint` en verde**, y ésa es toda la razón de que el escáner exista: la regla de ESLint mira **tipos**, y en `{ cod: "0003-0041184", deudaTotal: 3455.24 }` no hay ni un tipo escrito —el `number` lo infiere TypeScript—. `tsc` sí cae ahí, pero por otra cosa («TS2353: Object literal may only specify known properties, and 'deudaTotal' does not exist in type 'Recibo'») y esa red **desaparece en cuanto el campo existe**, que es el caso A; el escáner es el único que dice lo que pasa: `:96 deudaTotal (literal)`. **(C)** rojo por los dos lados —`lint` en `:79:19`, y la prueba con «declara `valor: number` sin la directiva de ESLint con su motivo en la linea de arriba: expected 0 to be greater than 20»—, que es lo que hace que la excepción sea una excepción y no un hueco: `valor` **entra** en `CAMPOS_DE_DINERO` sabiendo que hay uno declarado, en vez de dejarse fuera del selector. **(D) 2 en rojo desde dos ángulos que no se solapan**: la transcripción literal («expected ['2,055.40', …] to deeply equal ['2,055.04', …]») y la aritmética de la propia fila («la cuota 2024 · Impuesto predial · 1 a 4 …: expected 205504 to be 205540», en céntimos enteros porque `0.1 + 0.2` no es `0.3`). **Las cuentas del diseño cuadran de verdad**, y por eso valen como comprobación de la copia: las tres cuotas suman `2,511.94`, que es el importe del primer recibo, y los cuatro arqueos cuadran solos. **Y contarlos destapó una trampa del artboard**: la línea «Cobrado con tarjeta» lleva `+` y **no entra en la suma** —C-4 es `200.00 + 2,884.20 = 3,084.20` con sus `612.00` de tarjeta fuera—; está bien que esté fuera (ese dinero no está en el cajón) y en C-3 el detalle lo dice, pero en las otras tres **sólo lo dice la aritmética**, así que la prueba lo fija. **Los tres desajustes con el contrato se midieron contra el backend de este repositorio, no se copiaron del issue**: `NumeroDeRecibo` compone `"%s-%07d"`, o sea que el correlativo **sí** coincide con el diseño y lo que no coincide es el ancho de la serie, que **no lo fija nadie** (`serie varchar(5)`, de 1 a 5 caracteres: `0003` cabe y `1` también, luego una pantalla que reserve cuatro dígitos se rompe); y del **fondo inicial del turno** lo grave no es que `ArqueoResource` no lo publique, sino que su `diferencia` sale de `ArqueoDelTurno` como `declarado − neto`, de modo que **el fondo no está ni sumado por dentro** y un cajero que empiece con 200,00 en el cajón declarará +200,00 de diferencia contra un backend que no sabe de dónde salen — de los tres es el único que no se arregla con un rótulo. Las pruebas pasan de **75 a 96**, «`Test Files 6 passed (6)`» con el paréntesis leído, y **3 corridas con `--sequence.shuffle`, 96 de 96**. `yarn build` en verde, 30 módulos: **`src/datos/` todavía no llega al *bundle*** y es correcto —ninguna pantalla lo importa—, medido con `grep -c "0003-0041184" dist/assets/index-*.js` → **0**. **Y la copia no se dio por buena leyéndola**: se extrajeron las líneas 942-1215 a un módulo, se cargaron en Node y se compararon las **catorce constantes del artboard** contra `src/datos/`, una a una —**12 de 12 en verde**, incluidos los 48 pares de `HOJAS` y los trazos SVG—; para saber que esa comparación muerde, **un solo carácter** cambiado en un trazo (`12.8 0` → `12.8 1`) la pone en **2 rojos**, `MODULOS` e `ICO_MOD`. Esa prueba **no se versiona** y es correcto que no: el artboard no está en el repositorio, y una prueba que necesita un archivo que nadie tiene sale roja en CI por un motivo que no es el suyo |
| **#6 — la barra global** (`frontend/src/barra/`: `BarraGlobal.tsx` de 52 px con hamburguesa, escudo, entidad, campana, ejercicio, lupa, lanzador de nueve puntos y ficha de sesión; `AvisoDelSistema.tsx`, `Toast.tsx` con su reloj de 3 400 ms, los datos en `src/datos/barra.ts`, el estado en `App.tsx` y **28 pruebas nuevas** en dos archivos) | Tres roturas, cada una sola y restaurada por copia comparada con `cmp`: **(A)** quitarle al `useEffect` del toast su `return () => clearTimeout(reloj)`; **(B)** quitarle al subtítulo de la entidad su `data-sm-hide="1"`; **(C)** sacar `public/escudo-catacaos.png` de su sitio | **(A) 1 en rojo**: «expected 1 to be +0» en `expect(vi.getTimerCount()).toBe(0)` tras el `unmount()`. **(B) 3 en rojo desde tres ángulos**: «expected 'block' to be 'none'» a 700 px, a 760 px justos y en la sonda que afirma que happy-dom sí evalúa la consulta. **(C) 1 en rojo, y en el archivo que hace la petición de verdad**: «expected 'text/html' to contain 'image/png'» —mientras la aserción que lee el `src` del JSX **seguía verde**, 27 de 28—, que es exactamente por qué el criterio 3 exige no leer el JSX. **Y ejecutar el artboard en vez de leerlo destapó un hueco del diseño**: `seccionesStyle`, el estilo que su plantilla pide en la línea 106 para la hamburguesa, **no existe** — se extrajo su `<script type="text/x-dc">` a un módulo, se cargó en Node con un `DCLogic` de mentira y `renderVals()` devuelve `undefined` para esa clave, con `hasOwnProperty` en `false`. Qué pinta entonces el prototipo **no se ha medido** (haría falta su `support.js`, que no viaja con el archivo) y por eso no se afirma; la hamburguesa toma `lanzadorStyle`, que es el único estilo que el artboard define para un botón de esta barra que **alterna** algo. **Se retiró una aserción que no podía fallar por el motivo que decía comprobar**: el criterio 7 pide que tras el desmontaje no haya «aviso de React», y con la rotura (A) puesta el espía de `console.error` **siguió en verde** — React 18 retiró aquel «Can't perform a React state update on an unmounted component» y React 19 tampoco lo emite: la actualización sobre un componente desmontado se ignora en silencio. Contar los temporizadores es lo único que distingue el caso bueno del malo, así que es lo único que se afirma. **Tres medidas más que cambiaron cómo está escrita la prueba**, todas por ejecutar: jsdom **no evalúa** `@media (max-width: 760px)` —a 700 px devuelve `block`— mientras happy-dom sí, que es la segunda razón (junto a `var()`) de la excepción de entorno, y las dos están escritas como sondas que se pondrán rojas el día que sobren; la memoización del estilo calculado **no la invalida `setViewport`**, sólo tocar el DOM, así que cada prueba de ancho fija el ancho **antes** de dibujar; y `vi.advanceTimersByTime` **sin `act()`** dispara el `setState` pero no redibuja, de modo que la prueba del toast afirmaba «sigue ahí» sobre un DOM viejo — así escrita salía roja con el toast entero delante. **El servidor de desarrollo de Vite responde 200 con el `index.html` a cualquier ruta desconocida** —medido, la primera versión de esa prueba afirmaba lo contrario y salió roja—, así que el 200 no prueba nada: lo que prueba son el `content-type`, la firma PNG y el tamaño exacto, y por eso están los tres. **Un matiz del criterio 5**: dice «al descartarla desaparecen la banda y la campana», y la campana ya no está — el artboard la retira al **abrir** el aviso (`aviso: s.aviso && !s.avisoAbierto`, línea 1807), no al descartarlo; el final es el que pide el criterio y el camino es el del diseño. **El árbol arranca cerrado a propósito** aunque el artboard arranque con `secOpen: true`: hasta el issue del árbol, `aria-expanded="true"` sobre una región que no existe es algo que un lector de pantalla anuncia. Las pruebas pasan de **96 a 124**, «`Test Files 8 passed (8)`» con el paréntesis leído, y **3 corridas con `--sequence.shuffle`, 124 de 124**. `yarn build` en verde, 41 módulos. **Y se miró la pantalla en un Chromium de verdad, no en un emulador**: alto **52 px** exactos, fondo `rgb(0, 54, 90)`, escudo `/escudo-catacaos.png` a 36 px con tamaño natural **350×420**, el toast con `rgb(22, 35, 44)`, radio 7 px y su sombra, retirándose solo; a 700 px los **tres** `data-sm-hide` en `none`; **cero errores de consola y cero peticiones fallidas**. La copia se cotejó además contra el artboard **ejecutado**: 11 comprobaciones —entidad, los cuatro ejercicios, el contador y el rótulo de la campana, los dos títulos de la hamburguesa, los nueve puntos, el texto del toast para los cuatro años, los 3 400 ms de la línea 1275, el aviso de la 431 y la ficha de sesión de las 164-170— **11 de 11 en verde**, y muerde: cambiando **un dígito** del aviso (`534` → `543`) sale 1 rojo. Ese cotejo **no se versiona**, por lo mismo que en #5 |
| **#7 — el árbol de módulos y su filtro** (`frontend/src/arbol/ArbolDeModulos.tsx`: la **variante A** de las líneas 224-265, el filtro de las 209-222 y 308-312 y la cola de trabajo de las 362-373; `src/datos/cola.ts`, la clase `.hov-arbol` de `global.css`, el árbol enchufado en `App.tsx` con su `alIr` inyectado y **43 pruebas nuevas**) | Cuatro roturas, cada una sola y restaurada por copia comparada con `cmp`: **(A)** `cuantasAbiertas > 0` → `>= 0`, o sea la pastilla con un `0` dentro; **(B)** `abierto = hayFiltro || desplegado === rama.modulo` → sólo la segunda mitad, o sea un filtro que encuentra y no despliega; **(C)** que el filtro case sólo contra el **nombre del módulo** y no contra sus submódulos; **(D)** devolver `modulos: false` al arranque, que es la deuda de #6 sin pagar | **(A) 1 en rojo**, y por el nombre accesible: «Unable to find an accessible element with the role "button" and name "Tesorería"» — con la pastilla a cero el botón pasa a llamarse «Tesorería 0», que es justo lo que un lector de pantalla anunciaría. **(B) 2 en rojo**: «expected [ …(1) ] to have a length of 12 but got 1» y «expected false to be true». **(C) 3 en rojo**, «expected [] to deeply equal [ 'Tesorería' ]» — y lo que enseña es que el recuento **seguía diciendo «1 módulo · 1 submódulo»** sobre un árbol vacío: contador y árbol pueden contradecirse, y por eso se afirman los dos por separado. **(D) 30 en rojo en dos archivos**: «expected 'false' to be 'true'» en la hamburguesa y «Unable to find an accessible element with the role "complementary" and name "Módulos y submódulos"» en todo lo demás. **La copia no se dio por buena leyéndola**: se cargó el `<script type="text/x-dc">` del artboard en Node con un `DCLogic` de mentira y se comparó **lo que su `renderVals()` produce** contra lo que este port dibuja — `filtroConteo` y `sinCoincidencias` sobre **17 consultas**, la cola, el `asideStyle`, y **nueve estados** nodo a nodo: rótulos, `aria-expanded`, `aria-current`, la marca «abierta», el ` *` de las sucias, los trazos SVG y **todas las declaraciones de estilo en línea** de las seis piezas condicionales (botón de módulo, caja del icono, chevrón, pastilla, submódulo y marca), resolviendo cada `var(--token)` contra `tokens/`. **45 de 45 en verde**, y muerde: `padding: 7px 8px` → `7px 9px` en el botón del módulo la pone en **9 rojos de 9 casos** mientras la suite versionada sigue en **43 de 43 verde** — que es exactamente lo que esa suite no puede ver y por lo que el cotejo se hace. No se versiona, por lo mismo que en #5 y #6. **Y ejecutar el artboard resolvió el duplicado sin tener que creerse a nadie**: `cola` está escrita dos veces —líneas 1792 y 1823— y lo que `renderVals()` devuelve es la de 1823, «Cajas sin arquear» 2, «Sin conciliar» 11, «Anulaciones del día» 3. **Tres medidas del entorno que cambiaron cómo está escrita la prueba.** (1) happy-dom **rompe** `border-left: 1px solid var(--linea-2)`: al guardarla reparte el `var()` por las tres propiedades largas y tira el `1px solid`, así que allí el grosor **no se puede medir** — se mide el color, que sí llega (`#E3E9EE`), y el `1px` se comprobó en un Chromium de verdad: `1px solid rgb(227, 233, 238)`. (2) happy-dom devuelve **cadena vacía** para `position` cuando nadie la declara, de modo que un `toBe('static')` no podría fallar por el motivo que dice comprobar: se sustituyó por la estructura —mismo padre, fila `display:flex`, `flex: 0 0 252px`— y por la declaración en línea. (3) `campo.value = q` con un `Event('input')` **no llega a React**: el cotejo daba doce módulos con el filtro puesto hasta que pasó a `fireEvent.change`. **Una desviación de la plantilla, declarada**: el artboard dibuja a la vez el contenedor vacío de la variante A **y** el mensaje de «sin coincidencias», los dos con `flex:1`, lo que deja el mensaje centrado en la mitad de abajo; aquí es un `?:` porque los dos casos son excluyentes por construcción —`ramasVisibles` está vacía exactamente cuando no casa nada—. **Dos pruebas de `barra.test.tsx` cambiaron de signo con la deuda de #6**: la hamburguesa arranca ahora en `aria-expanded="true"` con «Ocultar los módulos», y la región que anuncia **existe**. **Y se miró la pantalla en un Chromium de verdad**: el panel mide **252×848** en `x:0` con el `<main>` empezando en `x:252` y midiendo 1 188; pulsar la hamburguesa lo lleva a **1 440**, o sea **exactamente 252 de diferencia** — el árbol empuja el contenido, no lo tapa, y eso es medida de disposición real, que ningún emulador de DOM da—; el módulo desplegado en `rgb(228, 244, 253)` con su icono en `rgb(0, 82, 132)` y su chevrón en `matrix(-1, 0, 0, -1, 0, 0)`; el submódulo activo en `#E4F4FD`/`#004670`/700; los tres puntos de la cola en `#C0492F`, `#C08A00` y `#C08A00`; el `style-hover` traducido a `.hov-arbol` ganándole al estilo en línea (`rgb(239, 247, 252)`); **cero errores de consola y cero peticiones fallidas**. Las pruebas pasan de **124 a 167**, «`Test Files 9 passed (9)`» con el paréntesis leído, y **3 corridas con `--sequence.shuffle`, 167 de 167**. `yarn build` en verde, 43 módulos. El conmutador del experimento no se portó: `grep -rn "panelVar\|esB\|esC" frontend/src` no devuelve nada, y lo vigila una prueba que recorre el árbol de archivos |
| **#8 — las pestañas, las rutas por hash y los estados vacíos** (`frontend/src/marco/`: la barra de pestañas de las líneas 380-401, la fila del título de las 403-416, el hueco de las 418-426, la tarjeta de pestaña ajena de las 438-464 y el diálogo de las 862-883; `usarPestanas` con el estado y sus transiciones, `hash.ts` con `replaceState`, `rotulos.ts` con `titulo`/`subtitulo`/`pestanas`, la ranura `Pantalla` por la que entrarán las cuatro pantallas, cuatro clases `hov-*` en `global.css` y **57 pruebas nuevas** en dos archivos) | Siete roturas, cada una sola y restaurada por copia comparada con `cmp`: **(A)** `quedan[Math.max(i - 1, 0)]` → `quedan[Math.min(i, quedan.length - 1)]`, o sea activar la vecina de la **derecha**; **(B)** `replaceState` → `pushState` en `marcarHash`; **(C)** que `pedirCierre` cierre siempre, sin mirar si está sucia; **(D)** quitarle al montaje la lectura del hash; **(E)** que abrir añada siempre, sin comprobar si ya estaba; **(F)** `padding: 11px 8px 11px 13px` → `…12px` en el botón de la pestaña; **(G)** que navegar deje de cerrar el lanzador y la paleta | **(A) 1 en rojo**: «expected 'territorio' to be 'panel'» — y es la rotura que más enseña, porque cerrar `panel` (i = 0) **sigue saliendo bien** con las dos fórmulas: el único caso que las separa es cerrar una de en medio, y por eso el criterio 3 se comprueba por los dos lados. **(B) 2 en rojo**: «expected 16 to be 8» en `history.length` y «expected "pushState" to not be called at all, but actually been called 4 times». Las dos hacen falta: `history.length` puede quedarse quieto por un entorno que no lo lleve, mientras que «nadie llama a `pushState`» es una propiedad del código. **(C) 6 en rojo**, todos «Unable to find an accessible element with the role "dialog"» o con el nombre de uno de los tres botones. **(D) 3 en rojo**: «expected 'Panel de Tesorería' to be 'Cajas y arqueo'» y «expected [ 'panel' ] to deeply equal [ 'panel', 'territorio' ]». **(E) 7 en rojo**, incluido «Found multiple elements with the role "button" and name "Cerrar Panel"» — dos pestañas iguales rompen antes el nombre accesible que el recuento. **(F) 2 en rojo en el cotejo contra el artboard y la suite versionada entera en verde**, que es exactamente lo que esa suite no puede ver. **(G) 2 en rojo**, «expected 'abierta' to be 'cerrada'» las dos: `ir` apaga el lanzador y la paleta (línea 1345) y eso vale para las **tres** puertas de entrada —el árbol, la barra de pestañas y «Cobrar»—, así que el cierre vive una sola vez en `App.irA` y no en cada una; la paleta no tiene diálogo hasta el issue siguiente, pero su estado sí es observable en `data-paleta`. **El duplicado de `ir` se resolvió ejecutando, no leyendo**: está escrita **tres veces** —líneas 1299, 1334 y 1343— y cargando el `<script type="text/x-dc">` en Node, `Component.prototype.ir.toString()` **no contiene** el comentario «Abrir un submódulo…» de la 1299: gana la última. Las tres tienen el mismo cuerpo, así que el duplicado es inerte — pero eso también es una medida. **Y ejecutar el artboard destapó un defecto suyo, que es la única desviación de este port**: `componentDidMount` con un hash válido hace `setState({ dest: inicial })` **sin tocar `abiertas`**, de modo que con `#cajas` queda `abiertas: ['panel']` y `dest: 'territorio'` —medido— o sea **una sección activa que ninguna pestaña representa**: la barra dibuja «Panel» con `aria-current="false"` y el título dice «Cajas y arqueo». Aquí el arranque y el `hashchange` externo pasan los dos por `abrirEn`, así que la activa siempre está abierta; sin esa invariante, «la vecina de la izquierda» —que se calcula sobre `abiertas`— no significa nada. **El criterio 6 pide recargar de verdad, y eso NO se puede hacer en la suite**: `location.reload()` no está implementado en jsdom, así que allí se mide lo más cercano —un **montaje nuevo** que lee `location.hash`, que es lo que una recarga produce en una aplicación de una sola página— y está dicho en la cabecera del archivo. La recarga de verdad se midió en Chromium: con `#tarifario` puesto, `page.reload()` devuelve **«Tarifario y cierre»**, y las pestañas pasan de `['panel','territorio','valores']` a `['panel','valores']` — o sea que **las abiertas no se persisten**, que es lo que el issue exige. Y ahí salió una trampa del arnés que habría dado por buena una medición falsa: **`page.goto(url + '#zzz')` desde `#cajas` NO recarga** —misma página, otro hash—, así que el estado de React sobrevive y la primera versión de la sonda «midió» que `#zzz` dejaba «Cajas y arqueo»; con un `about:blank` de por medio, las cuatro cargas dan lo mismo que jsdom, una a una. **Dos medidas del entorno que decidieron dónde vive cada prueba**: happy-dom dispara `hashchange` **tres veces** por una sola navegación (jsdom, una), así que el enrutado se prueba en jsdom; y happy-dom **rompe** `border-right: 1px solid var(--linea-2)` al leer el atributo `style` —reparte el `var()` por las tres propiedades largas y tira el `1px solid`, mientras que `border-top: 2px solid transparent`, **sin `var()`, se expande bien**—, así que el cotejo declaración a declaración corre en jsdom; ese `1px solid` que happy-dom pierde se comprobó en Chromium, donde el `border-right` del marco de una pestaña sale `1px solid rgb(227, 233, 238)`. **El cotejo contra el artboard, más ancho que el de #7**: `titulo` y `subtitulo` en ocho estados y en las **48 hojas**, `pestanas` —rótulo con su ` *`, `aria-current`, el `aria-label` del aspa y los trazos del icono— en los ocho, el texto de la tarjeta ajena en las 48, y **las cuatro piezas de estilo en línea** (marco, botón, icono y aspa) de una pestaña encendida y una apagada, con cada `var(--token)` resuelto: **120 de 120 en verde**. No se versiona, por lo mismo que en #5, #6 y #7. **Una decisión de diseño que el issue no pedía y hacía falta**: ensuciar una pestaña es `fijarCampo(k, v)` —el `set` de la línea 1352— y ninguna pantalla tiene campos todavía, así que `App` recibe la **pantalla** que dibuja la sección activa (por omisión el marcador). No es un interruptor de prueba: es la ranura por la que entran las cuatro pantallas, y lo que la prueba enchufa es un `<input>` que llama a `fijarCampo('caja', …)`, literalmente el `onTipo` de la línea 1926. **La banda del aviso se mudó** debajo de la barra de pestañas, que es donde el artboard la pone (línea 428) y donde `AvisoDelSistema` tenía escrito que iría cuando hubiera pestañas. **Y el marcador de posición de #3 se fue con él**: el `<h1>caja-web</h1>` lo sustituye el título de verdad, así que `andamio.test.tsx` pasa a afirmar que el `<title>` del HTML es `${MODULO} · ${NOMBRE_DE_LA_APLICACION}` — que es lo que mantiene una sola fuente de verdad para el nombre ahora que la página ya no lo pinta. Las pruebas pasan de **167 a 224**, «`Test Files 11 passed (11)`» con el paréntesis leído, y **3 corridas con `--sequence.shuffle`, 224 de 224**; el hash es global del documento, así que los tres archivos que montan `App` lo limpian en un `beforeEach` y por eso el orden no importa. `yarn build` en verde, 52 módulos. **Y se miró la pantalla en un Chromium de verdad**: la barra de pestañas mide **44 px** de alto en `x: 252` sobre `rgb(242, 246, 249)`, la activa va en `#fff` con filete `2px solid rgb(0, 82, 132)`, tinta `rgb(22, 35, 44)` y peso 700 y su icono en `rgb(0, 82, 132)`, mientras la apagada va transparente con `rgb(90, 107, 120)` y peso 400; el aspa de la activa en `rgb(90, 107, 120)` y la de las demás en `rgb(147, 163, 175)`, con el `style-hover` traducido a `.hov-aspa` ganándole al estilo en línea (`rgb(227, 233, 238)` y `rgb(22, 35, 44)`); cuatro navegaciones seguidas dejan `history.length` en **6 → 6**; el hueco conserva la franja de 44 px y **retira el `<h1>`**; y el diálogo —que sin campos no se alcanza, así que se midió sustituyendo `main.tsx` por uno que enchufa una pantalla con un campo, y restaurándolo con `cmp`— sale centrado (`556 px`, radio 10 px, sombra `rgba(0, 54, 90, 0.28) 0px 22px 56px`), con «Descartar y cerrar» en `rgb(143, 42, 23)` a la izquierda del todo, «Guardar y cerrar» en `rgb(0, 82, 132)` a la derecha, `Escape` haciendo lo mismo que «Seguir editando», y el toast «Cambios guardados en Panel.» al guardar. **Cero errores de consola y cero peticiones fallidas**, y el `dist/` servido con `vite preview` se comporta igual en las cuatro cargas, con la consola **completamente vacía** |
| **#9 — la paleta de comandos, el lanzador y el menú de sesión** (`src/paleta/PaletaDeComandos.tsx` y `acciones.ts` con las diez acciones de las líneas 1466-1477 y la plantilla de las 74-97; `src/barra/LanzadorDeModulos.tsx` con la rejilla de las 48-72; `src/barra/MenuDeSesion.tsx` con el menú de las 174-200 y su pie de sucias; `src/marco/usarAtajos.ts` con el `_tecla` de las 1246-1262; `src/marco/destino.ts` con el `extra` del `ir(dest, extra)`; `OPCIONES_DE_SESION` en `src/datos/barra.ts`; la clase `.hov-flotante`; **67 pruebas nuevas** en dos archivos y el arnés de navegador `verificaciones/paleta.mjs` enganchado a `frontend.yml`) | Siete roturas, cada una sola y restaurada por copia comparada con `cmp`: **(A)** quitarle a la paleta el manejo de `↓`, que es lo que el criterio 8 pide; **(B)** cambiar la guarda «al filtrar, el foco al primero» por una **acotación al último**, que es la trampa que `paleta.mjs` avisa; **(C)** quitarle a `usarAtajos` su `return () => window.removeEventListener(…)`; **(D)** reintroducir el defecto de la línea 1713 del artboard —`modulo.nombre === "Tránsito"`—; **(E)** quitar el `slice(0, 9)`; **(F)** que «Cerrar sesión» pierda su tinta y su peso y que el aviso de sucias pierda el singular; **(G)** `padding: 10` → `11` en el botón de módulo del lanzador | **(A) 6 en rojo** en la suite y **3 en el arnés**: «las flechas no mueven el foco», «↑ no vuelve a la entrada anterior» y «Intro no abrió «Arqueo de mi caja»: fue a territorio con nodo 0 (se esperaba territorio con nodo 2)» — el arnés nombra la fila que se abrió y la que debía abrirse, que es lo que hace leíble el fallo. **(B) 2 en rojo**, «expected 3 to be +0» y «expected 2 to be +0», y en el arnés «al filtrar, el foco se queda en una fila que nadie eligió: 0003-0041183 — Castillo Pascuala, María Elena». **Y la advertencia de `paleta.mjs` se midió, no se citó**: con la guarda quitada, una prueba escrita con **un solo resultado** —filtrar a «arqueo» y comprobar que el foco vuelve al primero— **pasa en verde**, porque acotar al último ya la salva; por eso la versionada usa nueve resultados y el foco en el cuarto. **(C) 1 en rojo**: «expected true to be false» sobre `defaultPrevented` de un `Ctrl+K` cancelable disparado tras el desmontaje. **Se midió también lo que NO sirve**: con el oyente colgado, la corrida entera **no emite un solo aviso de React** —cero `console.error`, cero «not wrapped in act»—, así que la aserción que el criterio 5 sugiere («ni produce aviso de React») no puede fallar y no se escribió; es el mismo hallazgo de #6 con otra fuga. **(D) 5 en rojo** desde cinco ángulos: «expected 'Tránsito' to be 'Tesorería'», el escáner de fuentes, «expected 'transparent' to be '#E4F4FD'», «expected 'Abriría el módulo Tesorería.' to be 'Ya está en Tesorería.'» y «expected 'Ya está en Tesorería.' to be 'Abriría el módulo Tránsito.'». **(E) 7 en rojo**, «expected […] to have a length of 9 but got 10». **(F) 4 en rojo**: «expected '#3A4A55' to be '#8F2A17'» y tres «expected 'Hay 1 pestañas…' to be 'Hay 1 pestaña…'». **(G) 8 rojos en el cotejo contra el artboard con la suite versionada entera en verde (67 de 67)**, que es exactamente lo que esa suite no puede ver. **El criterio 2 del issue está mal planteado, y la medida lo dice**: pide ver «las 10 acciones» con la consulta vacía y en la misma sección cita el tope de **9** de la línea 1484; las dos cosas no pueden ser ciertas. Ejecutando el artboard con un `DCLogic` de mentira, con `pq: ''` su `palRes` tiene **9** elementos y su `palPie` dice «9 resultados» — «Recibos anulados» queda fuera hasta que se teclea. Manda el diseño: el tope es 9, `ACCIONES` sigue teniendo las diez y hay una prueba de que la décima **se alcanza escribiendo**, que es lo que la hace no perdida. **El duplicado de `modulos` se resolvió ejecutando, no leyendo**: `renderVals().modulos` devuelve `actual: 'true'` en **Tesorería** (línea 1756), no en Tránsito (1713). Y el port **no escribe ninguna de las dos cadenas**: el módulo actual es `MI_MODULO`, y la nota «Tesorería» de las diez acciones también — lo vigila un escáner que, sobre `src/barra` y `src/paleta` **con los comentarios quitados**, prohíbe los doce nombres de módulo. Los comentarios se quitan porque el port **habla** del defecto de la 1713, que es como se evita repetirlo, y la primera versión del escáner se cazó a sí misma. **Una medida del entorno que ya había dado por buena una aserción falsa**: happy-dom pierde `min(620px,92vw)` de un estilo en línea, y las dos vías **no se comportan igual** — por `setAttribute` la función sobrevive en el atributo aunque `style.width` quede vacío; por asignación de propiedad, que es lo que React hace con un `style={{}}`, no queda ni rastro. La primera versión de la prueba midió la vía de `setAttribute`, afirmó sobre el atributo y salió roja contra el componente real. El ancho **de verdad** se midió en Chromium: el diálogo sale en `x: 410` con `width: 620` sobre 1 440 px, a `y: 108` que es 12vh exactos. **Y un hueco de `eslint.config.mjs` que sólo apareció al añadir el arnés**: su bloque de globales de Node casa `"*.mjs"`, que **es sólo la raíz**, así que `eslint .` sacó **12 `no-undef`** sobre `verificaciones/paleta.mjs` —`process`, `console`, y el `document` y el `URL` de los cuerpos que corren dentro del navegador—; el bloque nuevo es `verificaciones/**/*.mjs` con globales de Node **y** de navegador, porque un arnés de Playwright vive en los dos mundos a la vez. **Una asimetría del artboard, portada y fijada con su motivo**: `abrirSesion` apaga el lanzador y la paleta (1685) pero `abrirLanzador` sólo apaga la paleta (1759), así que con el menú de sesión abierto el lanzador se abre encima y quedan los dos; no se pisan —uno cuelga de `left:10px` y el otro de `right:0`— y lo que cuesta es un segundo clic para cerrar. Hay una prueba que lo afirma, para que separarse del diseño tenga que ser una decisión escrita. **Las cuatro pantallas siguen sin portarse, pero sus destinos sí**: cuatro de las diez acciones abren la misma sección y sólo se distinguen por `nodo`, y dos por `valTab`, así que `Destino` los lleva y la raíz los expone en `data-ir-nodo`, `data-ir-valtab`, `data-ir-chip` y `data-ir-recibo` — y lo que se afirma no es el número sino que la fila a la que lleva **se llama como la acción**: `NODOS[4]` es «Anulaciones del día» y `TARIFARIO[2]` es «Cierre y depósito». **El cotejo contra el artboard ejecutado**: los doce módulos con su `aria-current`, sus trazos, su `style` y su `iconStyle` declaración a declaración con cada `var(--token)` resuelto; las tres opciones de sesión igual; los quince toasts; `palRes` y `palPie` sobre **nueve consultas**; la pastilla de tipo; los nueve puntos y el `lanzadorStyle` apagado y encendido — **13 de 13 en verde**, y muerde con la rotura (G). No se versiona, por lo mismo que en #5, #6, #7 y #8. **Y se miró la pantalla en un Chromium de verdad**: el lanzador en `x:10, y:50` midiendo 560 px con `rgba(0, 54, 90, 0.2) 0px 18px 48px`, doce celdas de 182 px, Tesorería en `rgb(228, 244, 253)` con peso 700 y su icono en `rgb(0, 82, 132)` sobre blanco mientras los once llevan `rgb(228, 244, 253)`/`rgb(0, 70, 112)`; el menú de sesión en `x:1170, y:53` con 258 px, radio 9 px y `rgba(0, 54, 90, 0.22) 0px 16px 42px`, «Cerrar sesión» en `rgb(143, 42, 23)` con peso 600; la paleta con el campo **enfocado solo** (`document.activeElement`), `aria-activedescendant="paleta-opcion-0"`, nueve filas y «9 resultados», dos `↓` dejando «Ver los recibos del turno» e `Intro` llevando a `#recibos`; y el pie de sucias en `rgb(255, 244, 217)`/`rgb(122, 82, 0)` diciendo «Hay 1 pestaña…» y «Hay 2 pestañas…» —medido sustituyendo `main.tsx` por uno que enchufa una pantalla con un campo, y restaurándolo con `cmp`—. **Cero errores de consola y cero peticiones fallidas**, tanto en `yarn dev` como en el `dist/` servido con `vite preview`, donde el arnés también pasa. Las pruebas pasan de **224 a 291**, «`Test Files 13 passed (13)`» con el paréntesis leído, y **3 corridas con `--sequence.shuffle`, 291 de 291**. `yarn build` en verde, 58 módulos |
| **#10 — `#panel`, el Panel de Tesorería** (`frontend/src/pantallas/Panel.tsx` con la plantilla de las líneas 466-539; `src/datos/panel.ts` con `cifras`, `colaTotal`, `bandeja`, `cobertura` y `actividad` de las 1813-1860; `PantallaDeSeccion` como reparto de las cuatro secciones y omisión de la ranura `Pantalla` de `App`; `PropsDePantalla` gana `destino` e `irA`; `SECCION_DE_CAJAS` y `SECCION_DE_RECIBOS` en `datos/navegacion.ts`; la clase `.hov-fila` y **40 pruebas nuevas**) | Diez roturas, cada una sola y restaurada por copia comparada con `cmp`: **(A)** `S/ 27,693` → `S/ 27,690`, la que pide el criterio 1; **(B)** dibujar la pastilla del delta **siempre**, sin mirar si está vacío; **(C)** el nodo de «Sin conciliar» de 5 a 4, o sea una permutación que deja la sección correcta; **(D)** quitarle al rótulo del arqueo la rama de «cuadra»; **(E)** `toFixed(1)` → `toFixed(0)` en el ancho de la barra, o sea unificar los dos formatos; **(F)** que una fila de actividad abra `#recibos` **sin** el recibo; **(G)** escribir `S/ 27,693` en el JSX; **(H)** que `PantallaDeSeccion` dibuje el Panel en las cuatro secciones; **(I)** borrar la regla `.hov-fila` de `global.css`; **(J)** que la opacidad de la barra deje de escalar | **(A) 1 en rojo**: «expected 'S/ 27,690' to be 'S/ 27,693'». **(B) 1 en rojo**: «expected <span …(2)></span> to be null», con el recuento de dos pastillas de cuatro tarjetas al lado — sin ese recuento, dibujarlas las cuatro pasaría las dos primeras líneas. **(C) 2 en rojo desde dos ángulos que no se solapan**: «expected 'Anulaciones del día' to be 'Pendientes de conciliar'» —el criterio nombra **el título del nodo**, no su índice, y las tres filas van a la misma sección, así que comprobar sólo que llevan a `#cajas` dejaría pasar las tres permutaciones— y el cotejo contra la cola del árbol, «expected [ [ '2', 2 ], [ '11', 4 ], [ '3', 4 ] ] to deeply equal [ …, [ '11', 5 ], … ]». **(D) 2 en rojo**: «expected '100 %' to be 'cuadra'» las dos. **(E) 2 en rojo**: «expected '3%' to be '2.7%'» y «expected '0%' to be '0.2%'» — el segundo es el que enseña lo que costaría unificarlos: la barra de «Anulaciones» **desaparecería**, porque `0,2 %` redondeado a entero es un ancho de cero. **(F) 5 en rojo**, «expected '' to be '0003-0041184'» y sus tres hermanas; y lo que enseña es que la prueba de «Ver los recibos del turno» **sigue verde** con la rotura puesta, porque las cinco navegaciones van a la misma sección: es la lección de la paleta —la 1ª y la 3ª entrada iban las dos a `predios`— aplicada aquí. **(G) 4 en rojo desde tres ángulos**: dos tarjetas dibujando la cifra de otra («expected 'Su caja — C-352 recibosS/ 27,693Desde…' to contain 'S/ 9,419'»), el `grep` literal del criterio 7 y el escáner ancho («expected [ 'Panel.tsx: S/ 27,693' ] to deeply equal []»). **(H) 3 en rojo con las otras 328 pruebas en verde**, «expected <div data-seccion="panel" …> to be null» — el Panel sangrando en las cuatro secciones no lo ve nada más de la suite. **(I) 1 en rojo** sobre el texto de `global.css`. **(J) 1 en rojo**: «Fondo inicial: expected '1' to be '0.5'». **La copia no se dio por buena leyéndola**: se ejecutó el `<script type="text/x-dc">` del artboard con un `DCLogic` de mentira y se comparó lo que su `renderVals()` devuelve contra `src/datos/panel.ts` **campo a campo** —las cuatro cifras con su delta y su nota, las tres filas de la bandeja con su insignia resuelta a tono y su nodo, las cinco líneas del arqueo con su rótulo, su ancho y su nodo, y las cuatro de actividad con el recibo que abre cada una—: **verde**, y es lo que fija que `cobertura[3]` y `[4]` valen `'cuadra'` con `#1F5B39` mientras las tres primeras son `'3 %'`, `'98 %'` y `'0 %'` en `#5A6B78`, con opacidades `0.5`, `0.7` y `0.9`. No se versiona, por lo mismo que en #5, #6, #7, #8 y #9. **El criterio 6 no se puede cumplir entero, y está dicho**: pide que «el detalle de la ficha corresponda al código pulsado», y la ficha es `#recibos`, que **no se porta en este issue**. Lo que sí se afirma es lo que hará que ese día funcione: que pulsar cada fila deja `#recibos` con **ese** código en el destino, y que los cuatro códigos existen en `RECIBOS` con el estado y el tono que la fila anuncia — un código que no estuviera abriría una ficha vacía. **Dos cosas del artboard que se copian como están, con su motivo escrito donde vive el dato**: la primera fila de actividad dice `S/ 2,281.06` del recibo `0003-0041184` y ese mismo recibo vale `S/ 2,511.94` en `RECIBOS` —dos cifras para el mismo número, y elegir una sería decidir por el diseño cuál de las dos pantallas miente—; y el pie del arqueo escribe «se **conciliaba**» en pasado donde el resto del párrafo va en presente (`PORTAR.md`, regla 2). **El escáner del criterio 7 hubo que acotarlo dos veces, y las dos porque salió rojo por su cuenta**: buscando los textos del panel sobre el archivo entero encontró «Anulaciones» dentro de un comentario que explica por qué la barra no se redondea —una prueba que prohíbe nombrar en prosa lo que el código hace se acaba apagando—, así que mira el código **sin comentarios**; y «cuadra» casaba con la variable local `cuadra` de cada línea del arqueo, así que sólo se buscan los textos con un dígito o un espacio dentro. **Y su otra mitad estaba mal escrita**: «y sí en `src/datos`» como un `toContain` sobre el texto de `panel.ts` **pasaría con la tarjeta borrada**, porque ese archivo nombra las dos cifras en sus comentarios; se afirma sobre los valores de `CIFRAS` y `COBERTURA`. **Una medida que acota el criterio 5**: Chromium normaliza `width: 100.0%` a `100%` al leerlo, así que de las cinco barras sólo `2.7%` y `97.6%` sobreviven al viaje — que son justo las dos que el criterio nombra. En la suite se lee el atributo `style`, donde las cinco están tal cual. **Y que `App` pase a repartir de verdad se midió, no se supuso**: con `PantallaDeSeccion` de omisión, las **291** pruebas anteriores siguen verdes una a una, o sea que el Panel no le pisa a ninguna un nombre accesible ni un texto. Las pruebas pasan de **291 a 331**, «`Test Files 14 passed (14)`» con el paréntesis leído, y **3 corridas con `--sequence.shuffle`, 331 de 331**. `yarn build` en verde, 62 módulos, y el arnés de la paleta pasa contra el `dist/` servido (`exit 0`). **Y se miró la pantalla en un Chromium de verdad**, sobre `yarn dev` y sobre el `dist/`: las cinco barras miden **6,8 px, 245 px, 0,5 px, 251 px y 251 px** con `rgb(0, 82, 132)` a `0.5`/`0.7`/`0.9` las tres primeras y `rgb(31, 91, 57)` a `1` las dos que cuadran; los cinco rótulos dicen `3 %`, `98 %`, `0 %`, `cuadra` y `cuadra`, los dos últimos en `rgb(31, 91, 57)` y los tres primeros en `rgb(90, 107, 120)`; la rejilla de cifras se resuelve en `279px 279px 279px 279px`; el `style-hover` traducido a `.hov-fila` le gana al estilo en línea (`rgba(0, 0, 0, 0)` → `rgb(247, 251, 254)`); a 700 px el importe del arqueo queda en `display: none`; pulsar «Sin arquear» deja `#cajas` con `data-ir-nodo="2"` y el `<h1>` en «Cajas y arqueo», y pulsar `0003-0041182` deja `#recibos` con ese recibo. **Cero errores de consola y cero peticiones fallidas** en los dos |
| **#11 — `#recibos`, la lista** (`frontend/src/pantallas/Recibos.tsx` con la plantilla de las líneas 542-596 y el vacío de la derecha de las 599-607, y la lógica que las alimenta: el filtrado y el orden de las 1450-1458, `chips` (1867-1875), `orden` (1876-1877), `conteo` y `filas` (1878-1890) y `sinSeleccion` (1891); `CHIPS`, `ORDENES`, `ORDEN_NATURAL` y `TOTAL_DEL_TURNO` en `src/datos/recibos-del-turno.ts`; `predios` entra en el reparto de `PantallaDeSeccion`; y **55 pruebas nuevas**) | Catorce roturas, cada una sola y restaurada por copia comparada con `cmp`: **(A)** el conteo escrito como «N de N», que es el error que el criterio 2 nombra; **(B)** que el chip case sólo contra `estado` y no también contra `uso`; **(C)** `localeCompare(…, 'es')` → `a.titulo < b.titulo`; **(D)** que el orden «Recibo» ordene por `cod` descendente en vez de no ordenar; **(E)** el filtro con una disyunción en vez de una conjunción; **(F)** que la búsqueda mire sólo el título; **(G)** la fila elegida sin su borde izquierdo; **(H)** quitar la sincronización del chip que llega en el destino; **(I)** que pulsar una fila navegue **sin** el recibo; **(J)** que la lista no lea `destino.recibo`; **(K)** `padding: 11px 13px` → `11px 14px` en la fila; **(L)** que `PantallaDeSeccion` dibuje la lista en las cuatro secciones; **(M)** el vacío dibujado siempre; **(N)** `TOTAL_DEL_TURNO` de `52` a `51` | **(A) 11 en rojo**, con «expected '1 de 1' to be '1 de 52'» en el criterio 2 y «expected '5 de 5' to be '5 de 52'» en el 1. **(B) 1 en rojo —«expected false to be true»— con las otras 88 en verde, incluido el cotejo contra el artboard**, y eso es todo el hallazgo: los cinco recibos traen **el mismo texto en `estado` y en `uso`**, así que quedarse con uno da hoy exactamente la misma lista en los cuatro chips y **ningún dato real separa las dos implementaciones**; lo que las separa es un recibo con los dos campos distintos, y por eso `casaElChip` se exporta y se mide con uno inventado. **(C) 1 en rojo** —`«Ñañez Alva, Rosa»` saltando detrás de `«Zapata Rivas, Óscar»`— **y la prueba del criterio 6 con los cinco títulos reales sigue verde**: empiezan por C, D, I, S y Z, o sea que `<` y `localeCompare` dan la misma lista y el criterio, tal como está escrito, **no puede distinguirlos**; el caso que sí lo hace es la eñe al principio, que por unidades de código (U+00D1) cae detrás de la Z, y la prueba afirma **las dos** ordenaciones para que se vea que separan. **(D) 1 en rojo**, por lo mismo: el orden natural de `RECIBOS` coincide hoy con `cod` descendente, así que la lista real no separa «no ordenar» de «ordenar por código»; lo separa una lista desordenada, que el orden natural devuelve tal cual. **(E) 25 en rojo**, «expected [ Array(5) ] to deeply equal [ '0003-0041180' ]» y «Anulado + inversiones» dejando dos filas donde no debe dejar ninguna. **(F) 3 en rojo** —«expected [] to deeply equal [ '0003-0041182' ]» y su hermana del titular— **y «zapata» sigue verde**, porque casa por el título: de ahí que las tres consultas del criterio 2 estén elegidas **una por campo**. **(G) 2 en rojo desde dos ángulos**: «expected 'transparent' to be '#005284'» en la suite y «0003-0041182 · border-left: expected '3px solid transparent' to be '3px solid #005284'» en el cotejo. **(H) 1 en rojo**, «expected 'false' to be 'true'», **y el mismo caso abierto desde el panel sigue verde**: allí la pantalla se monta de nuevo y lee el chip del destino; lo que la sincronización arregla es la acción «Recibos anulados» de la paleta **estando ya en `#recibos`**, donde no hay montaje. **(I) 7 en rojo con las cinco de la deuda de #10 en verde**, y **(J) 8 en rojo con esas cuatro en rojo**: son las dos mitades del mismo mecanismo —quién escribe el destino y quién lo lee— y ninguna prueba cubre la otra. **(K) 1 en rojo en el cotejo con las 55 versionadas enteras en verde**, que es exactamente lo que esa suite no puede ver. **(L) 2 en rojo con las otras 420 en verde.** **(M) 1 en rojo**: dibujar el vacío siempre pasaría la prueba del criterio 3, y por eso está su otra mitad. **(N) 9 en rojo**, incluido el cotejo con la tarjeta del panel: «expected '52 recibos' to be '51 recibos'». **La copia no se dio por buena leyéndola**: se ejecutó el `<script type="text/x-dc">` del artboard con un `DCLogic` de mentira y se comparó **declaración a declaración, resolviendo cada `var(--token)` contra `tokens/`**, dibujando la pantalla con `renderToStaticMarkup` para que no hubiera CSSOM de por medio —happy-dom funde `border: 0` con `border-bottom` y `border-left` en un `border-width: 0px 0px var(--linea-2) var(--azul)` sin sentido, medido—: **los 28 estilos literales de la plantilla** (las 542-607, uno a uno con su selector), los **cuatro chips** con su rótulo, su `aria-pressed` y su estilo encendido y apagado, las **cinco filas** con su contenido, su `aria-current`, su estilo y su insignia, y el conteo, el orden, las filas y `sinSeleccion` en **18 estados** de consulta, chip, orden y recibo elegido — **34 de 34 en verde**, y muerde con la rotura (K). No se versiona, por lo mismo que en #5, #6, #7, #8, #9 y #10. **Lo que no entra, dicho**: la ficha de la derecha es de #12, así que con un recibo elegido queda un marcador —una mitad de pantalla en blanco es el modo de fallo silencioso que `PORTAR.md` avisa—; y el «Cobrar» del vacío deja el destino del cobro nuevo pero **sin su toast**, porque una pantalla no tiene con qué avisar y el formulario es de #13. **Dos cosas que salieron de ejecutar.** Con la lista vacía hay **dos botones que se llaman «Cobrar»** —el de la fila del título (410) y el del vacío (579)—, los dos del artboard: un `getByRole` sin acotar revienta con «Found multiple elements», así que la prueba busca dentro de la pantalla y queda escrito. Y el `.hov-fila` **le gana al fondo de la fila elegida**: con el ratón encima pasa de `rgb(228, 244, 253)` a `rgb(247, 251, 254)` —el `!important` que hace falta para vencer al estilo en línea, decidido en #6—, mientras el borde izquierdo se queda; medido en Chromium. **Una desviación del artboard, declarada**: la consulta, el chip y el orden viven en la pantalla y no en el marco, así que irse al panel y volver los reinicia; en el artboard viven en el estado global. Las pruebas pasan de **331 a 388**, «`Test Files 15 passed (15)`» con el paréntesis leído, y **3 corridas con `--sequence.shuffle`, 388 de 388**. `yarn build` en verde, 63 módulos, y el arnés de la paleta pasa contra `yarn dev` y contra el `dist/` servido (`exit 0` los dos). **Y se miró la pantalla en un Chromium de verdad**, sobre los dos: la lista mide **376 px en `x: 252`** con `border-right: 1px solid rgb(214, 222, 228)` sobre blanco y la mitad derecha **812 px**; el chip activo va `1px solid rgb(0, 82, 132)` sobre `rgb(228, 244, 253)` con tinta `rgb(0, 70, 112)`, peso 600 y radio 999 px, y el apagado `rgb(214, 222, 228)` sobre blanco con `rgb(90, 107, 120)` y peso 400; la fila elegida lleva **`border-left: 3px solid rgb(0, 82, 132)`** —los 3 px que happy-dom no puede decir— y la vecina `3px solid rgba(0, 0, 0, 0)`; el código y el importe salen los dos en `font-variant-numeric: tabular-nums`, el importe pegado al borde derecho de la lista; a **1200 px** la lista pasa a 320 y a **860** la fila se vuelve columna; «zapata» deja «1 de 52», `zzz` deja «0 de 52» con su «Cobrar» en `rgb(0, 82, 132)`, y pulsar `0003-0041180` en «Actividad reciente» del panel deja **esa** fila marcada. **Cero errores de consola y cero peticiones fallidas** en los dos |
| **#12 — `#recibos`, la ficha y el renderizador de campos** (`frontend/src/pantallas/FichaDelRecibo.tsx` con la cabecera de las líneas 609-621, las pestañas de las 654-663, la tabla de cuotas de las 710-741 y la barra inferior de las 767-771; `src/pantallas/CampoDeFicha.tsx` con la plantilla de las 671-706 y el `campo(f, d)` de las 1395-1409; `src/datos/valores-del-recibo.ts` con la rama de recibo existente de `datos()` (1379-1392); `PropsDePantalla` gana `avisar`, que es lo que #11 dejó declarado como hueco; y **77 pruebas nuevas**) | Trece roturas, cada una sola y restaurada por copia comparada con `cmp`: **(A)** el contador de pendientes **sin su `nuevo &&`**, que es el caso que separa el criterio 9; **(B)** el `ro` con borde continuo en vez de discontinuo; **(C)** `data-ancho` fijo a `"0"`, o sea `ancho` ignorado; **(D)** «Anular el recibo» avisando **sin** saltar a la sección 5; **(E)** «Ver la cuenta corriente» **navegando**, como si la cuenta corriente fuera de este sistema; **(F)** el botón derecho diciendo siempre «Continuar»; **(G)** «Anterior» apagado siempre; **(H)** la palabra «opcional» sin dibujarse nunca; **(I)** todas las columnas de la tabla a la derecha; **(J)** el campo dibujado pero sin llamar a `fijarCampo`; **(K)** «Anular el recibo» dejando de ser la acción primaria; **(L)** `padding: 11px 12px 9px` → `…10px` en la pestaña; **(M)** la cabecera de la tabla toda a la izquierda | **(A) 3 en rojo, y es la que más enseña**: «expected 'Anulación2' to be 'Anulación'». El contador del artboard es `nuevo && f > 0` (línea 1948), así que quien se dejara el `nuevo &&` **vería un «2» en la quinta pestaña** — porque `faltan` da `[0, 0, 0, 0, 2]` con estos valores: «Motivo» y «Autorizado por» están vacíos y son obligatorios. Una prueba que sólo dijera «no hay contadores» **pasaría igual con la implementación equivocada en las otras cuatro secciones**, y por eso la suite afirma también las dos cifras. **(B) 5 en rojo**, uno por cada `ro` de «Operación»: «expected 'solid' to be 'dashed'». Y va con su otra mitad —los cuatro campos que **no** son `ro` tienen borde continuo—, porque un `dashed` puesto a todos pasaría los cinco. **(C) 3 en rojo** desde tres ángulos: «expected '0' to be '1'» sobre el atributo, «expected [] to deeply equal [ 'contrib', 'obsOp' ]» sobre el `grid-column` calculado de los nueve campos, y «expected '' to be '1 / -1'» en las otras tres secciones. **(D) 1 en rojo**: «expected [ 'operacion' ] to deeply equal [ 'anulacion' ]» — el toast salía igual, así que comprobar sólo el texto no habría visto nada. **(E) 1 en rojo**: «expected [ 'anulacion' ] to deeply equal [ 'operacion' ]». Es la mitad simétrica: «no navega» es una afirmación, y sin ella «saca su toast» la cumplirían las dos. **(F) 3 en rojo**: «expected undefined to be defined» y dos «Unable to fire a "click" event - please provide a DOM element», que es lo que pasa cuando el botón que la prueba busca por su rótulo no existe. **(G) 1 en rojo**: «expected 'true' to be 'false'» — el criterio nombra sólo la primera sección, y un `aria-disabled="true"` fijo la cumpliría; lo que lo separa es la segunda. **(H) 8 en rojo**, uno por cada opcional de las cinco secciones. **(I) 1 en rojo**: «expected [ 'right', 'right' ] to deeply equal [] ». **(J) 3 en rojo**: «expected 'Recibos' to be 'Recibos *'» en la pestaña y en el árbol, y «expected '' to be 'Atendido en ventanilla 2.'» al leer lo escrito de vuelta. **(K) 1 en rojo**: «expected '#fff' to be '#005284'». **(L) 25 rojos en el cotejo contra el artboard con las 465 versionadas enteras en verde**, que es exactamente lo que esa suite no puede ver. **(M) 1 en rojo**: «expected [ 'left', 'left', 'left' ] to deeply equal [ 'right', 'right', 'right' ]» — y esa aserción **se añadió por la rotura (I)**, que dejó la cabecera desalineada de su columna sin poner nada rojo. **La copia no se dio por buena leyéndola**: se ejecutó el `<script type="text/x-dc">` del artboard con un `DCLogic` de mentira y se comparó **declaración a declaración, resolviendo cada `var(--token)` contra `tokens/`** y dibujando con `renderToStaticMarkup` para que no hubiera CSSOM de por medio — los estilos literales de la plantilla (609-771, uno a uno y en el orden del documento), `INS`, `IN`, `IN_MAL`, `TH`/`THN`/`TD`/`TD1`/`TDN`, los tres `ficha.acciones[].style`, los cinco `tabs[].style` y el `atrasStyle`/`adelanteStyle` **en las cinco secciones**: **317 de 317 en verde**, con **una desviación declarada** que el propio cotejo nombra y explica —el artboard repite la familia en el `<textarea>` (línea 692) como `'Source Sans 3',sans-serif`, **sin `system-ui`**, mientras su `<style>` global la reparte con `system-ui` y `font: inherit`; el port usa el token, que es esa segunda, para que el área de texto no caiga en otra fuente que los demás controles cuando la web no carga—. No se versiona, por lo mismo que en #5 a #11. **Y ejecutar el artboard resolvió dos cosas que leerlo no resuelve.** (1) `datos()` **no recibe el recibo elegido** (línea 1367): devuelve el mismo objeto para los cinco, así que al abrir el recibo anulado de Zapata Rivas la cabecera cambia y el cuerpo **no** —«Contribuyente» sigue diciendo Rufina Medina y «Número» sigue siendo `0041184`—. Medido, no supuesto, y se porta tal cual con su motivo escrito donde vive el dato: repartirlos por recibo sería **inventar cuatro juegos de datos** que el diseño no trae, y un dato inventado en una pantalla de cobro no se distingue del real. (2) `state.intento` **sólo lo enciende la emisión del cobro nuevo** (línea 2021), de modo que **el estilo de error `IN_MAL` no se puede alcanzar desde esta pantalla**: por eso `estiloDeCampo` es una función pura con `intento` como argumento y se mide como tal —las tres condiciones y sus tres negaciones—, y hay además una prueba que recorre las cinco secciones pulsando todo y comprueba que ningún campo se pinta mal. Una prueba que sólo pulsara botones no podría distinguirla de una que devolviera siempre `IN`. **Una medida del entorno que decidió cómo se comprueba el criterio 2**: happy-dom **rompe el atributo `style`** de un `border: 1px dashed var(--borde-campo)` —lo serializa como `border: 1px dashed` y le añade `border-width: var(--borde-campo)`, `border-style: var(--borde-campo)` y `border-color: var(--borde-campo)`, medido con una sonda antes de escribir la prueba—, mientras el **calculado** sale entero: `dashed`, `1px`, `#C3CFD9`. De ahí que el criterio se compruebe sobre `getComputedStyle` y no sobre el atributo, que es además lo que el issue pide. **Una traducción declarada**: el mapa de campos del marco es `Record<string, string>` desde #8, así que una casilla guarda `'1'` marcada y `''` sin marcar donde el artboard guarda `true`/`false`; la equivalencia es exacta porque una casilla **nunca es obligatoria** (línea 1398) y por tanto ese `''` no puede encender el estilo de error de nadie. **Y un hueco de #11 cerrado**: `PropsDePantalla` gana `avisar`, que es lo que hacía falta para que «Anular el recibo» y «Ver la cuenta corriente» saquen su toast —#11 lo dejó escrito como la razón de que su «Cobrar» no avisara—. Las pruebas pasan de **388 a 465**, «`Test Files 16 passed (16)`» con el paréntesis leído, y **3 corridas con `--sequence.shuffle`, 465 de 465**. `yarn build` en verde, **66 módulos**, y el arnés de la paleta pasa contra el `dist/` servido (`exit 0`). **Y se miró la pantalla en un Chromium de verdad**, sobre `yarn dev` y sobre el `dist/`: el código en `14px`/`700`/`rgb(0, 82, 132)` con `tabular-nums`; las tres acciones con la primaria en `rgb(0, 82, 132)` sobre blanco y las dos de borde en `1px solid rgb(214, 222, 228)`; las cinco pestañas con **un solo hijo cada una** —o sea ningún contador— y la activa con `2px solid rgb(0, 82, 132)`, `rgb(22, 35, 44)` y peso 700; la rejilla `repeat(auto-fit,minmax(212px,1fr))` resolviéndose en `248px 248px 248px` con `gap: 14px 16px`, y `contrib` y `obsOp` en `grid-column: 1 / -1` mientras los otros siete quedan en `auto`; el `ro` en `1px dashed rgb(195, 207, 217)` sobre `rgb(247, 251, 254)` con radio 6 px y `tabular-nums`; la tabla con `min-width: 780px`, 3 filas, 6 columnas y las tres últimas celdas en `right` con `tabular-nums` mientras las tres primeras quedan en `start` y sin cifras tabulares; «Anterior» en la primera sección con `aria-disabled="true"` y tinta `rgb(147, 163, 175)`. **Cero errores de consola y cero peticiones fallidas** en los dos |
| **#13 — `#recibos`, el cobro nuevo: la barra, la validación que bloquea y la emisión** (`frontend/src/pantallas/CobroNuevo.tsx` con la barra de caja y contribuyente de las líneas 623-652, el resumen «Lo que se va a registrar» de las 743-763 y la lógica pura de las 1428-1448, 1924-1942 y 1969-2008; `FichaDelRecibo` pasa a dibujar **las dos** ramas del `hayFicha` de la 1892 —el contador de pendientes de la 1948, la tabla vacía de la 1964, `bloqueado`/`motivo`/`adelante` de las 2014-2027—; `src/datos/cobro-nuevo.ts` con la rama `esNuevo()` de `datos()` (1368-1378); `usarPestanas` y `PropsDePantalla` ganan `limpiarCampos`, que es el `vals: {}` de `nuevo()` (2075), de `abrir(cod)` (2081) y de «Descartar» (1914); `abrir(cod)` recupera además su `paso: 0`, y **se retira una prueba de #12 que afirmaba en falso lo contrario**; y **65 pruebas nuevas y una retirada**, que dejan la suite en 529) | Diecinueve roturas, cada una sola y restaurada por copia comparada con `cmp`: **(A)** `puede` sin su `!cajaCerrada`, que es el criterio central; **(B)** el motivo con el documento por delante de la caja cerrada; **(C)** el documento sin el filtro de dígitos; **(D)** el documento sin el corte a once; **(E)** el mínimo de dígitos puesto en once en vez de en el de la caja; **(F)** el contador de pendientes sin su `> 0`; **(G)** `bloqueado` sin la condición de la última sección; **(H)** el resumen dibujado en las cinco secciones; **(I)** la tercera línea del resumen fundida en dos redacciones en vez de tres; **(J)** la emisión sin comprobar `puede`; **(K)** la emisión que avisa pero **no** enciende el intento; **(L)** la tabla de cuotas con las tres filas del recibo de muestra; **(M)** «Descartar» sin tirar lo escrito; **(N)** `nuevo()` sin tirar lo escrito; **(O)** un píxel de relleno en la barra de caja; **(P)** el aviso del código siempre en los colores del aviso normal; **(Q)** `abrir(cod)` sin tirar lo escrito; **(R)** `abrir(cod)` sin volver a la primera sección; **(S)** el `paso` clavado en 0 | **(A) 2 en rojo**, y lo que enseña es el rojo que daba **antes** de reforzarla: «expected 'Recibo — emitido. La cuota ya está descontada de la cuenta corriente.' to be 'La caja elegida está cerrada: no se puede emitir en ella.'» — o sea que la caja se emitía **un recibo cuyo número es un guion**, que es exactamente lo que el comentario de las líneas 1428-1430 dice que no puede pasar. Y destapó un hueco: la prueba que mide `aria-disabled="true"` con la caja cerrada **seguía en verde**, porque allí faltan además nueve datos y el botón se bloquea igual por otro motivo; el único caso que separa las dos implementaciones es **la caja cerrada con todo lo demás lleno**, y esa aserción se añadió por esta rotura (ahora el rojo es «expected 'false' to be 'true'»). **(B) 1 en rojo** —«expected 'Falta el documento del contribuyente.' to be 'La caja elegida está cerrada: no se puede emitir en ella.'»— **y solo en la prueba de la función pura**: en la pantalla la caja cerrada se prueba siempre con el documento entero, así que ningún estado real separa los dos órdenes; lo separa una caja cerrada **sin** documento, y por eso `motivoDe` se exporta y se mide con los cuatro casos. **(C) y (D) 2 en rojo cada una**, «expected 'abc123def' to be '123'» y «expected '123456789012345' to be '12345678901'»: son **las dos mitades** de `soloDigitos` y ninguna cubre a la otra —el `maxlength` del campo no para un pegado por programa y el filtro no para un teclado numérico—. **(E) 645 en rojo**, 12 en las versionadas y 633 en el cotejo, con «expected '8 de 8 dígitos mínimo' to be 'Contribuyente localizado'» y «expected '—' to be '0003-0041185'»: es la rotura que separa el **once del campo** del **ocho de la caja**, que el issue pide que no sean el mismo número. **(F) 3 en rojo**, «expected [ '0', '1', '2', '1', '2' ] to deeply equal [ '', '1', '2', '1', '2' ]» — y **el cotejo no la ve**, porque en sus cinco escenarios ninguna sección llega a cero. **(G) 21 en rojo** (1 versionada, 20 en el cotejo): «operacion: expected 'true' to be 'false'». El bloqueo del artboard tiene **tres** condiciones (línea 2014) y la de «última sección» es la que deja recorrer el borrador; sin ella el botón nace apagado en «Operación». **(H) 101 en rojo** (1 y 100), «operacion: expected `<section data-resumen="1">` to be null». **(I) 5 en rojo** (1 y 4), «expected ': no entra al arqueo' to be 'Sin medio de pago elegido'» — con el medio sin elegir, fundir las redacciones deja la pantalla diciendo «: no entra al arqueo», que además de ilegible **afirma sobre dinero** lo que nadie ha decidido. **(J) 7 en rojo**, incluido «expected 'Recibo 0003-0041193 emitido…' to be 'Quedan 9 datos obligatorios sin llenar.'» y el mismo «Recibo — emitido» de la (A). **(K) 4 en rojo**, «motivoAnul: expected 'width: 100%; box-sizing: border-box; …' to contain 'border: 1px solid #A8321E'»: es la deuda que #12 dejó declarada —`state.intento` no lo encendía nadie y `IN_MAL` era inalcanzable—, y **este issue es quien la enciende**. **(L) 36 en rojo** (1 y 35), «expected …(2) to have a length of +0 but got 3». **(N) 1 en rojo**, «expected '12345678' to be ''». **(O) 25 en rojo, todas en el cotejo y ninguna en las 527 versionadas**, que es exactamente lo que esa suite no puede ver. **(P) 6 en rojo** (1 y 5), «expected '#F7FBFE' to be '#FBE4E0'». **(Q) 1 en rojo**, «expected 'C-4 — abierta · turno tarde' to be 'C-3'». **(R) 1 en rojo**, «expected [ 'anulacion' ] to deeply equal [ 'operacion' ]», y **(S) 64 en rojo**: son las dos mitades del `paso: 0`, la que lo pone y la que impide ponerlo siempre. **Y (M) dio 0 en rojo, que es el hallazgo de esta tanda**: el `vals: {}` de «Descartar» **no se puede observar**, porque descartado el borrador la mitad derecha no dibuja ningún campo y las **dos** únicas salidas de ese estado —empezar otro cobro y abrir un recibo— vuelven a tirar lo escrito. Se porta igual, porque es lo que la línea 1914 escribe, pero queda dicho en el código y en la prueba que lo que el criterio 10 cubre es **la pareja** y no esa mitad; contarlo como cubierto sería justo la prueba que no puede fallar por el motivo que dice comprobar. **La copia no se dio por buena leyéndola**: se ejecutó el `<script type="text/x-dc">` del artboard con un `DCLogic` de mentira y se comparó **declaración a declaración, resolviendo cada `var(--token)` contra `tokens/`** y dibujando con `renderToStaticMarkup` — la plantilla entera (609-771) en **cinco escenarios × cinco secciones** (sin documento, con siete dígitos, con ocho, con la caja `C-1` cerrada y con `Efectivo` elegido), más los textos que la lógica compone y los campos que el intento pinta: **2 195 de 2 195 en verde**, y las 317 de #12 siguen enteras. No se versiona, por lo mismo que en #5 a #12. **Y ejecutar el artboard decidió cuatro cosas que leerlo no decide.** (1) `bloqueado` vale `"false"` en las secciones 0 a 3 **aunque la caja esté cerrada**: el bloqueo existe solo donde está el botón que cobra, y el criterio 4, tal como está escrito, se cumpliría con un `aria-disabled="true"` fijo que dejaría el borrador sin poder recorrerse; por eso la prueba afirma **las dos** mitades. (2) Un cobro nuevo nace con **nueve** obligatorios vacíos repartidos `[3, 1, 2, 1, 2]`, y dos de ellos son «Motivo» y «Autorizado por» **de la sección de anulación**: para emitir hay que llenar la anulación de un recibo que todavía no existe. Es del diseño, se porta tal cual y se deja dicho. (3) La emisión pone en `predio` un código —`0003-0041193` con ocho dígitos— que **no es ninguno de los cinco recibos del turno**, así que la ficha desaparece y la mitad derecha vuelve al «Elija un recibo de la lista»; medido (`hayFicha` pasa a `false`), portado tal cual, porque inventarle datos al recibo emitido sería inventar la única pantalla donde un dato inventado no se distingue del real. (4) **`abrir(cod)` hace `paso: 0` y `vals: {}`, y #12 no portó ninguna de las dos: aquí se reponen las dos y se retira la prueba suya que defendía lo contrario.** El estado que `abrir` deja, medido ejecutando la lógica del artboard en Node, es `{"predio":"0003-0041180","paso":0,"vals":{},"intento":false}`. La mitad de `vals` importa desde este issue: la barra guarda la caja y el documento, así que un recibo abierto tras un cobro enseñaría en «Caja» y en «Documento» los del cobro (rotura Q, 1 en rojo). La de `paso` importa por lo que significa: sin ella se abre un recibo y se aterriza en «Anulación» —la sección que da de baja un cobro— porque es donde dejó el anterior (rotura R, 1 en rojo: «expected [ 'anulacion' ] to deeply equal [ 'operacion' ]»). **Y esa segunda mitad tenía enfrente una prueba versionada de #12**: `ficha.test.tsx:783-793`, con el `describe` «la seccion elegida sobrevive a cambiar de recibo, **como en el artboard**» y el comentario «El `paso` del artboard vive en su estado global (linea 1221) y **nadie lo reinicia al cambiar de recibo**» — que es exactamente lo que la línea 2081 desmiente. Se retira entera, y no se le corrige el comentario, porque lo que estaba mal no era la prosa sino la aserción: defendía como comportamiento del diseño justo lo que el diseño evita. En su sitio quedan tres pruebas —abrir otro recibo aterriza en «Operación», **cambiar de sección dentro del mismo recibo sigue funcionando**, y abrir otro recibo no hereda lo escrito—, y la segunda es la que separa las hipótesis: con el `paso` clavado en 0 (rotura S) caen **64**, incluidas las dos primeras del trío, mientras que sin la pareja un `paso: 0` fijo dejaría la ficha inservible y en verde. **Y la tercera lección es la que vale para el siguiente**: la primera búsqueda de esa prueba —`grep -n "paso\b" verificaciones/ficha.test.tsx`— **no la encontró**, y con ese vacío se dio por bueno que no existía. No la encuentra porque la aserción **no nombra `paso`**: va por el ayudante `seccionActiva()`, que es quien lee `data-paso`. La que la destapa es `grep -n "sobrevive\|seccionActiva()).toEqual"`. Un `grep` sobre el nombre de una variable no ve las pruebas escritas a través de un ayudante, y **un `grep` vacío no es una medición**: la medición fue aplicar el cambio y correr la suite, que dio 1 en rojo con su línea y su fichero. **Dos medidas del entorno.** happy-dom, al serializar `border: 1px solid var(--borde-campo)`, le añade tres declaraciones longhand con el `var()` dentro, y esas tres **sobreviven** cuando React reescribe el borde a `1px solid #A8321E`: como van detrás, ganan, y el calculado sigue diciendo `#C3CFD9` con el atributo diciendo `#A8321E`; de ahí que el criterio 7 mida el borde **sobre el atributo** y el fondo sobre el calculado. Y Playwright trata `aria-disabled="true"` como deshabilitado y se queda esperando, mientras que el navegador **sí** manda el click —que es lo que el artboard necesita para encender el intento—, así que el arnés lo pulsa con `force`. **Y se miró la pantalla en un Chromium de verdad**, sobre `yarn dev` y sobre el `dist/` servido, cobrando uno de punta a punta con la caja abierta y con la cerrada: la barra en `rgb(247, 251, 254)` con `12px 18px`; la insignia «Borrador» en ámbar y la del código pasando de `0 de 8 dígitos mínimo` a `Contribuyente localizado` en verde; el `<code>` en `rgb(0, 70, 112)` sobre `rgb(228, 244, 253)` con `tabular-nums`; el campo del documento en **130 px** (`8 × 13 + 26`) y su borde recorriendo los tres estados `rgb(192, 138, 0)` → `rgb(195, 207, 217)` → `rgb(168, 50, 30)`; las cuatro cajas ofrecidas, **las dos cerradas incluidas**; el aviso de caja cerrada sobre `rgb(251, 228, 224)` con `3px solid rgb(168, 50, 30)`; los cinco contadores en `rgb(255, 244, 217)` sobre `rgb(122, 82, 0)` y radio `999px`, bajando a ninguno; el resumen con sus cuatro líneas y su pie pasando de `rgb(251, 228, 224)` a `rgb(220, 239, 227)`; el botón `aria-disabled="true"` con `opacity: 0.55` y su `title`; los obligatorios vacíos en `1px solid rgb(168, 50, 30)` sobre `rgb(255, 249, 248)` **y los opcionales no**; la tabla de cuotas con 0 filas, sus 6 cabeceras y su texto centrado; y el cobro completo dejando «Recibo 0003-0041193 emitido. La cuota ya está descontada de la cuenta corriente.». **Cero errores de consola y cero peticiones fallidas** en los dos. Las pruebas pasan de **465 a 529** —62 nuevas en `cobro.test.tsx`, y `ficha.test.tsx` de 77 a 79: una retirada y tres puestas—, «`Test Files 17 passed (17)`» con el paréntesis leído, y **3 corridas con `--sequence.shuffle`, 529 de 529**. `yarn build` en verde, **68 módulos**, y el arnés de la paleta pasa (`exit 0`). **Una desviación declarada más**: el subtítulo del resumen (línea 747) habla de «una ficha registrada» que «entra en el padrón» y de un «predio» que «genera obligación predial» — es prosa de la plantilla de Catastro sobre la que V6 se dibujó, no de un cobro. Se copia literal, como el nombre `PROGRAMAS`, y se anota donde vive para que se corrija **en el artboard** |
| **#14 — `#cajas` y `#tarifario`, las dos pantallas que faltaban** (`frontend/src/pantallas/Cajas.tsx` con la plantilla de las líneas 779-817 y la lógica de `arbol` (2034-2043) y `terr` (2044-2049); `src/pantallas/Tarifario.tsx` con la de las 822-856 y `valTabs`/`val` (2052-2066); `src/pantallas/TablaDeDatos.tsx`, que saca de `FichaDelRecibo` los `TH`/`THN`/`TD`/`TD1`/`TDN` (929-933) y el reparto de `cols()`/`filas()` (1360-1365) —tres sitios los usan, como en el artboard— y añade la tabla de cabecera pegajosa; `PantallaDeSeccion` **pierde su `default`** y `src/marco/MarcadorDeSeccion.tsx` pasa a `src/marco/pantalla.ts`, sólo el contrato; el arnés de navegador `verificaciones/pegajosa.mjs` con su `yarn pegajosa`, enganchado a `frontend.yml`; y **79 pruebas nuevas** en dos archivos) | Catorce roturas, cada una sola y restaurada por copia comparada con `cmp`: **(A)** la pantalla sin leer `destino.nodo`, que es la deuda de #10 sin pagar; **(B)** sin sincronizar el destino, o sea el mismo caso estando ya en `#cajas`; **(C)** la primera columna sin su `TD1`; **(D)** la cabecera toda a la izquierda; **(E)** «la última columna» en vez de «la numérica»; **(F)** la tabla sin `data-sticky`; **(G)** el contenedor de `#cajas` con `overflow: visible`; **(H)** la píldora «Solo lectura» sólo en la primera pestaña; **(I)** el pie repitiendo la nota de cabecera; **(J)** las tres pestañas dibujadas como activas; **(K)** un píxel de relleno en el botón del nodo; **(L)** `acotar` sin su cota de abajo; **(M)** `7,420.41` escrito en el JSX; **(N)** el tarifario sin leer `destino.valTab` | **(A) 81 en rojo** —7 versionadas y 74 en el cotejo—, y por el nombre del nodo: «expected [ 'C-3 — su caja, abierta' ] to deeply equal [ 'C-1 — cerrada ayer sin arquear' ]». Es la deuda de #9 y #10, que **hasta este issue nadie recogía**: siete entradas —tres filas del Panel y cuatro acciones de la paleta— abrían la misma sección y sólo se distinguían por su `nodo`. Lo que se afirma es **el título del nodo que queda elegido**, no su índice: los seis van a `#cajas`, así que «lleva a `#cajas`» no distingue el 2 del 5. **(B) 1 en rojo**, «expected [ 'C-1 — cerrada ayer sin arquear' ] to deeply equal [ 'Pendientes de conciliar' ]» — y es la mitad que (A) no cubre: son dos mecanismos distintos, leer el destino al montar y sincronizarlo **sin montaje**, que es lo que pasa cuando la paleta cambia de nodo estando ya en `#cajas`. **(C) 49 en rojo** (1 y 48): «expected 'normal' to be '600'». **(D) 12 en rojo** (2, 8, y **1 en `tarifario.test.tsx` y 1 en `ficha.test.tsx`**): «expected [ 'left', 'left', 'left', 'left' ] to deeply equal [ 'left', 'left', 'left', 'right' ]». Ese rojo de `ficha.test.tsx` es la medida de que sacar `cabeceraDe`/`celdaDe` a un módulo compartido **no aflojó lo que #12 protegía**: la prueba de la tabla de cuotas sigue mordiendo sobre el código movido. **(E) 33 en rojo** (1, 1 y 31), y es la que más enseña: **el criterio 3 sigue verde entero**. En las cuatro tablas de arqueo la columna numérica **es** la última, así que ningún dato de esa pantalla separa «la numérica» de «la última»; lo que las separa son los dos listados —`Importe S/` es la 3ª de 6 en anulaciones y la 5ª de 6 en pendientes— y el `Tasa S/` del tarifario, que es la 4ª de 5. Por eso el criterio 4 y el 8 afirman **cuál** columna va a la derecha y no cuántas. **(F) 4 en rojo** (3 y 1): «expected '' to be 'sticky'», «expected '' to be '2'» y «expected null to be '1'» — y el arnés de navegador sale con **código 1** diciendo «la cabecera se fue con la tabla — de y=243.84375 a y=183.84375 tras desplazar 60 px», en las dos pantallas. **(G) 7 en rojo** (1 y 6), «expected 'visible' to be 'auto'», **y es la rotura que justifica el arnés**: quitarle el desplazamiento al contenedor deja los `<th>` con su `position: sticky` intacto —la declaración no cambia— y la cabecera se va igual. Al medirla en Chromium el arnés dijo «#cajas: el contenedor no se desplazó (scrollTop 0): no se ha medido nada», que es **la guarda que se escribió antes de necesitarla**: sin ella, «la cabecera no se movió» habría salido cierto por no haber movido nada, y el arnés habría dado verde sobre el defecto. **(H) 2 en rojo**, «expected null not to be null» en las dos pestañas que no son la primera. **(I) 8 en rojo** (5 y 3): «expected 'Las tasas que se cobran en ventanilla…' to be 'El derecho de emisión es la única tas…'». **(J) 8 en rojo** (2 y 6): «expected '#005284' to be 'transparent'». **(K) 36 en rojo, todas en el cotejo y ninguna en las 608 versionadas**, que es exactamente lo que esa suite no puede ver. **(L) 1 en rojo**: «expected -1 to be +0». **(M) 3 en rojo** (2 y **1 en `panel.test.tsx`**): «Cajas.tsx: expected 'import { useState, type CSSProperties…' not to contain '7,420.41'» y «expected [ 'Cajas.tsx: 7,420.41', …(1) ] to deeply equal []» — el tercero es del escáner de #10, que ya prohibía esa cifra. **(N) 34 en rojo** (2 y 32): «expected [ 'Tarifario del TUPA' ] to deeply equal [ 'Cierre y depósito' ]». **La copia no se dio por buena leyéndola**: se ejecutó el `<script type="text/x-dc">` del artboard con un `DCLogic` de mentira y se comparó **declaración a declaración, resolviendo cada `var(--token)` contra `tokens/`** y dibujando con `renderToStaticMarkup` para que no hubiera CSSOM de por medio — los estilos literales de las dos plantillas en el orden del documento, `arbol[].style` y `valTabs[].style` en los **seis nodos y las tres pestañas**, los cinco estilos de celda repartidos por `cols()` y `filas()`, y además los textos: los seis rótulos con su recuento, los `aria-current`, el título y la nota de cada nodo, la nota de cabecera y la de pie de cada pestaña, los dos trazos del candado y **todas las celdas de las nueve tablas, una a una y en su orden**: **557 de 557 en verde**, y muerde con la rotura (K). No se versiona, por lo mismo que en #5 a #13. **Y ejecutar el artboard destapó un defecto suyo, que es la única desviación de este port**: acota la tabla con `Math.min(s.nodo, DETERMINACIONES.length - 1)` (líneas 1460 y 1464) pero marca el nodo con `s.nodo === i` (2035), de modo que **los dos índices se pueden separar** — medido con `nodo: 9`, `terr.titulo` da «Pendientes de conciliar» y **ninguno de los seis botones queda con `aria-current="true"`**: una tabla que nadie eligió, que es el mismo defecto que el arranque con hash de #8. Aquí el índice se acota **una sola vez** y lo usan los dos, con una cota por abajo que el artboard no tiene: sin ella un índice negativo deja `DETERMINACIONES[-1]` y con él la mitad derecha en blanco, que es la forma silenciosa de fallar que `PORTAR.md` avisa. **Una afirmación falsa que estuvo escrita y hubo que retirar**: la primera versión de `tarifario.test.tsx` medía los `2px` y el `solid` del filete de la pestaña activa, razonando que al declarar `border: 0` y **un solo** `border-bottom` happy-dom no tendría tres propiedades largas que fundir. Salió roja —«expected '' to be '2px'»— y la sonda posterior dijo por qué: de `border: 0; border-bottom: 2px solid var(--azul)` happy-dom guarda `border-width: 0px 0px var(--azul)` y `border-bottom-style: var(--azul)`, o sea que el `var()` se cuela en el sitio del grosor **y** del estilo y los dos salen cadena vacía. Lo que sí llega entero es el color. Quedan dos sondas —una por pantalla— que afirman que **no se puede medir**, para que el día que happy-dom lo arregle salgan rojas y la excepción sobre; los `2px` y los `3px` se midieron en Chromium. **El escáner del criterio 10 también salió rojo por su cuenta, y lo que encontró no era una fuga**: «Se conciliaba» dentro de `CobroNuevo.tsx`, que es el `COMO_SE_CONCILIA` que #13 copió del artboard —«Se conciliaba contra el extracto del banco…»— y cuyos trece primeros caracteres coinciden con el título de una columna del tarifario. Se acotó a lo que el criterio de verdad prohíbe: el texto **escrito entero**, entre comillas o como texto de JSX, no como principio de una frase más larga. **Y una rotura enseñó a leer el paréntesis otra vez**: la primera versión de (H) dejaba un `{elegida === 0 && (` sin cerrar, y Vitest imprimió «`Test Files 14 failed | 6 passed (20)`» con «`Tests 96 passed (96)`» — noventa y seis de noventa y seis *passed* sobre catorce archivos que no cargaron. Las pruebas pasan de **529 a 608**, «`Test Files 19 passed (19)`» con el paréntesis leído, y **3 corridas con `--sequence.shuffle`, 608 de 608**. `yarn build` en verde, **70 módulos**, y los dos arneses pasan contra `yarn dev` y contra el `dist/` servido (`exit 0` los cuatro). **Con esto `MarcadorDeSeccion` se retira**: no le llega nadie, y lo que protege hoy es el `switch` exhaustivo, que se pone rojo en `tsc` el día que aparezca una quinta sección. La prueba de #10 que afirmaba «y `%s` sigue en el marcador» —reducida a dos casos por #11— se queda con la lista **vacía**, y un `it.each([])` no es una prueba que pase sino una que no existe: se sustituye por su complementaria, que cada sección dibuja **la suya**, marcada con su propio `data-seccion`. **Y se miró la pantalla en un Chromium de verdad**, sobre `yarn dev` y sobre el `dist/`: la lista de `#cajas` mide **300×752 en `x: 252`** con `border-right: 1px solid rgb(214, 222, 228)` sobre blanco y la mitad derecha 888; los seis nodos de 40 px, el elegido con `border-left: 3px solid rgb(0, 82, 132)` —los 3 px que happy-dom no puede decir— sobre `rgb(228, 244, 253)` y peso 700, y los otros cinco `3px solid rgba(0, 0, 0, 0)` sobre transparente y peso 400; los `<th>` en `sticky`/`top: 0px`/`z-index: 2` sobre `rgb(247, 251, 254)`, en mayúsculas y con `letter-spacing: 0.77px`; la primera columna con `''`, `+`, `+`, `−`, `=`, `''`, `=` **los siete en peso 600**; la última en `right` con `tabular-nums` y `rgb(22, 35, 44)`; el `.hov-fila` ganándole al estilo en línea en la fila **y en el nodo** (`rgb(247, 251, 254)`), dejándole al elegido su borde azul; «Anulaciones del día» con 3 filas y 6 columnas y `Importe S/` —la tercera— a la derecha, «Pendientes de conciliar» con 4; y pulsar «Sin conciliar» en el panel dejando **«Pendientes de conciliar»** elegido. En `#tarifario`, la pestaña activa con `2px solid rgb(0, 82, 132)`, `rgb(22, 35, 44)`, peso 700 y relleno `12px 14px 10px` y las otras dos con `2px solid rgba(0, 0, 0, 0)` y `rgb(90, 107, 120)`; la píldora de **109×28** con radio 999 px sobre `rgb(247, 251, 254)`, borde `rgb(214, 222, 228)` y tinta `rgb(90, 107, 120)`; la tabla con `min-width: 660px` y `Tasa S/` —la cuarta de cinco— a la derecha con `tabular-nums` mientras `Clasificador`, que es la última, queda en `start`; y las tres pestañas con su nota y su pie distintos. A **1200 px** la lista pasa a 320 y a **860** el split se vuelve columna. **Cero errores de consola y cero peticiones fallidas** en los dos |
| **Las catorce tablas de `catastro` que este reparto no nombraba, y la regla 11 dejaba de revisar** (`DE_CATASTRO` pasa de 16 a **30**: `catastro_evento` —el buzón de salida, sin nombrar desde C-8—, las cuatro de `V7`, las tres de `V8`, las cinco de `V9` y `frente_derivacion` de `V10`) | **La rotura es un contraste de dos mitades, y hay que hacer las dos**: escribir en `src/main` de `caja` un cruce de verdad —`SELECT r.id FROM recibo r JOIN catastro_evento e ON e.predio_id = r.id`— y correr la frontera **con** la tabla en el reparto y **sin** ella. Los dos archivos restaurados por copia comparada con `cmp` | **1 en rojo con la tabla nombrada, y `BUILD SUCCESSFUL` sin ella.** El rojo dice el defecto entero: «la tabla «catastro_evento» es de «catastro» y esto es «caja»: el dia que la base se parta, esta consulta deja de funcionar en produccion y no antes. Se pide por un puerto, o se registra como cruce consentido con el issue que lo cierra: `JOIN catastro_evento`». **Y el verde es la mitad que importa**: es el MISMO cruce, en el MISMO archivo, y pasa sólo porque la tabla no estaba en el mapa — el reparto se consulta con `getOrDefault(tabla, SISTEMA_REPLICADO)` y «replicado» significa «no está a ningún lado de la frontera», así que una tabla que **falta** no da un cruce: **deja de revisarse**. Es la lección de R-N por el eje de las tablas, y no es hipotética: `catastro_evento` llevaba **sin nombrar desde C-8**, o sea que el buzón de salida de `catastro` no lo vigilaba nadie en ninguno de los cuatro sistemas. Lo destapó el censo que `catastro#7` escribió del lado del dueño (`ningunaTablaDelEsquemaSeQuedaFueraDelReparto`), que **allí** encontró cinco huecos; contadas contra los tres repartos de aquí eran **catorce**. Nombrar de más no cuesta nada —ningún archivo de este repositorio menciona ninguna de las catorce, medido— y es exactamente lo que hace que el cruce, si llega, se vea. **Y se comprobó que ninguna colisiona**: ninguno de los tres esquemas crea una tabla con esos nombres (`acta` es la del hallazgo de `catastro`; la tributaria de `rentas` se llama `acta_fiscalizacion` y sigue siendo suya) |
| **#15 — responsive, impresión, accesibilidad y los dos arneses que faltaban** (`@media print` con su `@page` A4 y el `data-cromo` de las cuatro piezas del marco en `global.css`, `BarraGlobal`, `ArbolDeModulos`, `BarraDePestanas` y `FichaDelRecibo`; el `!important` del anillo de `:focus-visible`; el `aria-label` de la búsqueda de `#recibos`; `verificaciones/mirar.mjs` y `verificaciones/cero-red.mjs` con sus dos trabajos en `frontend.yml` y las capturas como artefacto; **25 pruebas nuevas** en `envoltura.test.tsx` y una de `foco.test.ts` que cambia de signo) | Once roturas, cada una sola y restaurada por copia comparada con `cmp`: **(A)** un `fetch("/caja/api/v1/recibos")` dentro de una pantalla, que es lo que el criterio 8 pide explícito; **(B)** quitarle a la barra de pestañas su `data-cromo`; **(C)** borrar el bloque `@media print` entero; **(D)** quitarle el `!important` al anillo de `:focus-visible`; **(E)** **el árbol como superposición del handoff** —`position: fixed` fuera del flujo—, que es la corrección del enunciado; **(F)** quitar `table { min-width: 0 }` de la impresión; **(G)** quitar el `@page`; **(H)** mover el corte de la lista de 1240 a **1180**, que es el del conmutador del prototipo; **(I)** quitarle el `aria-label` a la búsqueda; **(J)** cegar `cero-red.mjs` quitándole la regla que reconoce un `fetch`; **(K)** renombrar el `aria-label` del lanzador, o sea romper un paso del recorrido | **(E) 4 en rojo**, y es la que el issue exige que se vea: «a 1400 px ocultar el arbol ensancha el contenido en **0 px y no en 252**: o no empuja, o el panel no mide lo que dice» y «a 1400 px el arbol esta `position: fixed`: fuera del flujo no empuja, tapa», las dos también a 1000. **(A)** rojo por los dos lados y en este orden: `yarn lint` en `src/pantallas/Recibos.tsx:86:6` —«Esta interfaz no habla con nadie: sin fetch y sin XMLHttpRequest»— y, construyendo sin lint, `cero-red.mjs` con **exit 1** y la URL delante: «5 peticiones de conexion (fetch / XHR / EventSource / WebSocket): `fetch http://localhost:5273/caja/api/v1/recibos`» ×5, una por carga. **(C) 2 en la suite** —«la pieza «barra» no casa con ninguna regla de `@media print`»— y **20 en el arnés**, incluidas las tres piezas de cromo dibujándose en las cuatro secciones, «27 elementos sobresalen del ancho imprimible de 703 px» en `#cajas` y «la hoja no es un A4 vertical sino **612x792 pt**». **(D) 4 en el arnés**, uno por campo —«select «Ejercicio de trabajo» enfocado no ensena el anillo rgb(82, 189, 239): outline «3px none rgb(0, 70, 112)»»—, 1 en `envoltura` («expected '' to be 'important'») y 1 en `foco.test.ts` («expected 'none' to be 'solid'»). **(G) 4 en rojo**, «la hoja no es un A4 vertical sino 612x792 pt» en las cuatro: sin `@page`, Chromium cae a Letter y **el PDF sale igual de bien a la vista** — el único sitio donde la diferencia se ve es la `/MediaBox`, y por eso se lee de ahí. **(H) 1 en rojo**, «expected '376px' to be '320px'» en el caso de 1 240 px justos. **(K) 1 en rojo**, «1 pasos del recorrido no se pudieron hacer, asi que ese camino no se midio: el lanzador de modulos: TimeoutError». **Y tres roturas dieron menos rojos de los que debían, que es donde estaba el trabajo de verdad.** **(B)** dejó `mirar.mjs` **en verde** con la suite en 2 rojos: el arnés trataba «esa pieza no está» como «esa pieza no toca en esta pantalla» —cierto para la barra de acciones, falso para las otras tres—, así que una pieza que **pierde su marca** se imprimiría sin que el arnés dijera nada. Con la lista de piezas obligatorias por sección, la misma rotura da **4 rojos**: «no hay ningun `[data-cromo="pestanas"]` en la pagina — o la pieza no se dibuja, o ha perdido su marca y por tanto se imprimiria». **(F)** dio **0 rojos**: el recorrido elegía un recibo y se quedaba en la primera sección de la ficha, donde no hay tabla, así que «nada sobresale» era cierto por no estar dibujada la tabla más ancha de la aplicación —la de cuotas, `min-width: 780px` contra los 703 px de un A4—; abriendo la sección que la trae, la misma rotura da **11 rojos** («table hasta x=799»). **(J)** dio **verde**: la sonda se veía salir, pero el guarda solo preguntaba si había aparecido, y una petición al propio origen aparece igual en el cajón de «recursos propios»; ahora se exige que aparezca **en el cajón de conexiones**, y la rotura da «la sonda no se conto como peticion de conexion: este arnes esta CIEGO y su cero no significa nada. Se esperaba `/caja/api/v1/__sonda-del-arnes` en el cajon «conectadas»; aparecio en «propias»». **(I)** también enseñó algo al salir verde la primera vez: quitarle el `aria-label` a la hamburguesa no la deja muda —le queda su `title`, que es nombre accesible—, así que la rotura que separa es la del campo, y ésa sí da 1 rojo: «input placeholder=«Recibo, contribuyente o documento»». **Lo primero que hizo este trabajo fue medir lo que ya estaba, y cinco de los once criterios ya se cumplían sobre `main`**: los cortes de 1240, 900 y 760 entraron con `global.css` en #4 y el árbol empuja desde #7. Lo que faltaba era impresión, los dos arneses y **una cosa que nadie había mirado**: el anillo de foco. **El anillo `#52BDEF` no llegaba a ningún campo**, medido en Chromium sobre esta misma aplicación — `input:focus` vale (0,1,1) y `:focus-visible` a secas (0,1,0), así que el `outline: none` de la línea 25 del artboard ganaba **también con el foco del teclado**, que es justo cuando el anillo tiene que verse. Y lo que quedaba en su lugar era medio repuesto: de las dos señales que el artboard le da al campo enfocado, la del **borde** `#005284` es inerte porque los estilos van en línea —el filtro del árbol enfocado sale con `border-color: rgb(22, 35, 44)`, su `currentColor`, y el `<select>` de orden con `rgb(214, 222, 228)`—, de modo que al campo le quedaba **solo** el `#D3EBFA`, 1,2:1 sobre blanco. El arreglo es un `!important` en el anillo, con el mismo argumento que ya obliga al de las clases `.hov-*` desde #6 —el estilo base va en línea, que es la doctrina, y una regla que un estilo en línea apaga es una regla escrita que no se ve nunca—, y hace falta además contra los **tres `outline:none` en línea** del propio artboard: el de la paleta (80), el del filtro (212) y el de la búsqueda (549). **Con eso una prueba de #4 cambia de signo**: `foco.test.ts` afirmaba «y se quita el `outline` del navegador» y ahora afirma que el campo recibe **además** el anillo del acento; no se le corrige el comentario, se sustituye la aserción, porque lo que estaba mal no era la prosa sino lo que defendía. **La impresión NO sale del artboard, y conviene que se sepa**: su `<style>` son las líneas 15-43 y ahí no hay `@media print` ni `@page` —medido con `grep`, no supuesto—. Lo que sí sale del diseño es qué es cromo y qué es contenido, y por eso lo que se retira son las cuatro piezas del marco marcadas con `data-cromo` y no una lista de etiquetas: con `header, aside, …` una pieza nueva se imprimiría en silencio. **Cuatro medidas del entorno que decidieron cómo está escrita cada prueba.** (1) happy-dom **sí** aplica `:focus-visible` a un campo enfocado con `focus()` —es lo que puso roja la prueba de #4—, pero **tira el `@page`**: no aparece entre las reglas del `CSSMediaRule` de `print`, así que el tamaño de la hoja solo se puede medir en Chromium. (2) Y sigue sin saber expandir un `outline: … var(--token)`: lo reparte por las tres propiedades largas y deja `outline-width: var(--acento)`, igual que hace con `border` desde #7 — queda una sonda que se pondrá roja el día que lo arregle. (3) Un `.click()` sobre el nodo dispara el manejador pero React **no redibuja** fuera de `act()`, así que la ficha no llegaba a existir y la pieza de cromo «acciones» no aparecía: `fireEvent.click`. (4) El servidor de desarrollo sirve el CSS **como módulo de JavaScript**, así que por el origen propio no llega ni una hoja de estilos —tipos propios `document, image, script` sobre `yarn dev` contra esos tres más `stylesheet` sobre `vite preview`—, y el guarda de «la página cargó» lo distingue en vez de poner rojo el modo desarrollo por cómo sirve Vite. **Y una trampa del arnés que ya había dado por buena una medición falsa**: el recorrido con `Tab` abría la segunda pestaña **pulsándola**, y pulsar deja el foco en lo pulsado, así que el recorrido empezaba por la mitad y declaraba «inalcanzables» los once controles que quedaban por encima —los once alcanzables—. Un `blur()` no lo arregla: Chromium se queda con el punto de partida de la navegación secuencial donde estaba, medido, los mismos once. Se llega por el hash, que además deja **dos** pestañas abiertas, que es lo que hace que «cada pestaña queda alcanzada» signifique algo. **Las medidas, sobre `yarn dev` (5274) y sobre el `dist/` servido con `vite preview` (5273), las dos idénticas**: la lista de `#recibos` mide **376 px a 1440 y 320 a 1000**, y en la suite **320 a 1 240 justos y 376 a 1 241**, que es lo que fija dónde está el corte; a **880** el split pasa a `column` con la lista en **628×300** y `max-height: 300px` sobre un contenido de 628; los **cuatro** `data-sm-hide` se ven a 880 y **ninguno** a 740; el árbol mide **252 px en x=0 con `position: static`** y ocultarlo lleva el contenido de **1 148 a 1 400 y de vuelta a 1 148** a 1400 px, y de **748 a 1 000 a 748** a 1000 — *exactamente* los 252 que mide el panel, que es lo único que distingue «empuja» de «tapa»; con `reducedMotion: 'reduce'` las **tres** animaciones del artboard declaradas a la vez (`fadeIn` del Panel, `subir` del toast y `pop` del lanzador) salen las tres a **1e-05s**; `Tab` recorre **42 controles y vuelve al documento** —sin trampa de foco—, **38 con el anillo `rgb(82, 189, 239)` y 4 campos con el suyo además**, y **0 inalcanzables** de los que el DOM nombra; las cuatro secciones dibujan **1 557, 705, 773 y 734 caracteres** con **0 quejas de consola**; y en papel salen fuera las cuatro piezas de cromo, quedan **1 500, 1 670, 773 y 734 caracteres**, **0 elementos sobresalen** del ancho imprimible de 703 px y la hoja mide **594,96×841,92 pt**, que es A4 vertical, en **2, 2, 1 y 1** hojas. `cero-red.mjs` cuenta **20 peticiones propias, 10 a la tipografía declarada, 0 de conexión y 0 a terceros sin declarar** sobre el `dist/`, y 270 propias sobre `yarn dev` con sus WebSockets de HMR nombrados como lo que son. **La tipografía se permite y se nombra**, no se deja pasar: `fonts.googleapis.com` (la hoja) y `fonts.gstatic.com` (los ficheros), cualquier otro anfitrión pone el arnés en rojo; y una petición fallida a esos dos **se informa y no falla**, porque la pila de respaldo de `--familia` termina en `system-ui` y una ventanilla sin salida a internet tiene que dibujar la pantalla igual. Autohospedarla es otra decisión y otro issue. Las pruebas pasan de **608 a 634**, «`Test Files 20 passed (20)`» con el paréntesis leído, y **3 corridas con `--sequence.shuffle`, 634 de 634**. `yarn build` en verde, **70 módulos** —ninguno nuevo: esto es envoltura— con el CSS de **3,04 kB a 3,42 kB** (gzip 1,27 → 1,39), y **los cuatro arneses pasan contra `yarn dev` y contra el `dist/` servido** (`exit 0` los ocho) |
| **#16 — la imagen `kamayuk-caja-interfaz`** (`frontend/Dockerfile` en dos etapas —`node:22-alpine` que compila, `nginx:1.31.4-alpine` que sirve `dist/` con `USER 101` y `HEALTHCHECK`—; `frontend/nginx.conf` **sin ningún reenvío al backend**, con `try_files`, caché y gzip; `frontend/.dockerignore`; y la tercera entrada de la matriz de `publicar-imagenes.yml`, con `archivo` y `contexto` **parametrizados**, más las tres etiquetas en `comprobar`) | **Esta máquina no tiene Docker** —ni demonio, ni `podman`, ni espacios de nombres de usuario: `unshare --user` da «Operation not permitted»—, así que **los criterios 1, 2, 3, 4 y 6 no se comprobaron con `docker build`/`docker run`, y eso queda dicho**. Lo que sí se hizo es traerse la imagen base **de verdad**: las ocho capas de `nginx:1.31.4-alpine` (digest amd64 `sha256:1f25fedd…`) bajadas de `registry-1.docker.io` con `curl`, y su binario **ejecutado** con su propio cargador musl (`ld-musl-x86_64.so.1 --library-path`), que contesta `nginx version: nginx/1.31.4`. Sobre eso, dos arneses. (1) **El nginx real sirviendo el `dist/` real con el `nginx.conf` del repositorio**, del que sólo se reescriben **dos líneas** —`listen` y `root`, porque no puedo crear `/usr/share/nginx/html`—, comprobado con un `diff` que no enseña ninguna más. (2) **Un replay de la etapa final del Dockerfile** sobre una copia del rootfs extraído: lee las instrucciones del archivo y las aplica una a una (`RUN rm`, los dos `COPY`, el `sed`), salta `chown`/`chmod` porque no soy root, y **resuelve** el `USER` contra el `/etc/passwd` de la imagen, que es lo que hace Docker. Nueve roturas, cada una sola y restaurada por copia comparada con `cmp`: **(A)** quitarle a `location /` su `try_files`; **(B)** meter una directiva de reenvío de verdad; **(B')** meter la palabra **sólo en un comentario**; **(C)** quitar el `USER`; **(D)** `COPY --from=construccion /origen/dist/` → `/origen/`; **(E)** quitar el vaciado de `/usr/share/nginx/html`; **(F)** quitar la tercera entrada de la matriz; **(G)** volver `file` y `context` fijos en el paso; **(H)** quitar la interfaz del bucle de `comprobar`; **(I)** quitar `node_modules` del `.dockerignore` | **(A) el criterio 3 en rojo**: `404 Not Found` de nginx y `curl -sf` saliendo **22** — **y el criterio 2 sigue en verde, 200**, porque `index index.html;` resuelve `/` él solo: pedir la raíz **no puede** ver un `try_files` que falta, y el único caso que separa las dos implementaciones es una ruta inventada. **(B) 1** y **(B') 1 también**, que es el hallazgo: `grep -c proxy_pass` **no distingue una directiva de la prosa que la explica**, y la primera versión de este `nginx.conf` —que explicaba por qué no tiene reenvío— daba **3**, con cero reenvíos dentro. El criterio 5, tal como el issue lo escribe, **habría salido rojo sobre un archivo correcto**; se resolvió como #10 resolvió su escáner, no nombrando la directiva en la prosa, y queda escrito en el propio archivo para que nadie la vuelva a escribir. La forma robusta —`grep -v '^\s*#' … \| grep -c`— da **0** con la prosa y **1** con la rotura (B). **(C)** «la etapa final NO declara `USER`»: la configuración de `nginx:1.31.4-alpine` trae **`User` vacío** —leído del JSON de configuración de la imagen—, o sea que sin esa línea la interfaz correría como **root**. **(D) el criterio 6 en rojo y a gritos**: `ls /usr/share/nginx/html` pasa de 3 entradas a **14**, con `node_modules`, `src`, `package.json`, `yarn.lock` y **37 archivos `.tsx`** dentro de la imagen. **(E)** `ls` devuelve `50x.html` además de las tres: el resto de la imagen base, que es por lo que el `rm -rf` está. **(F)** «entradas de la matriz = 2», sin `kamayuk-caja-interfaz`. **(G)** el paso construye con `backend/Dockerfile` y contexto `.`, donde **la etapa `interfaz` no existe**. **(H)** «se publican pero NO se preguntan: `['kamayuk-caja-interfaz']`» —con el nombre del trabajo diciendo «las tres», que es dos afirmaciones contradiciéndose—. **(I)** «faltan `['node_modules']`», y lo que cuesta son **172 MB** de contexto. **Y hadolint 2.15.1 encontró un defecto que ninguna de las nueve roturas veía**: `DL3066`, «non-numeric user-id may not be resolvable». No es cosmético — `infrastructure/src/descriptor.ts` endurece **todo** contenedor con `runAsNonRoot: true` («el endurecimiento que no admite excepción», #157), y con un `USER nginx` el kubelet **no puede comprobar** que no es root y se niega a arrancarlo, con un error que sólo aparece al desplegar — que es justo lo que #17 va a hacer. Por eso es `USER 101`, el uid que la base crea con `adduser -S -D -H -u 101` y que su `/etc/passwd` confirma (`nginx:x:101:101:…`), igual que las dos del backend ya usan `10001` y `10002`; con eso y el `HEALTHCHECK` en forma JSON, `hadolint` sale en **0**. **Las medidas del criterio 2 y el 3, contra el nginx de verdad**: `GET /` da `200`, `Content-Type: text/html`, `Cache-Control: no-cache` y un cuerpo **idéntico byte a byte** a `dist/index.html` (`cmp` en 0) — y lo mismo `/cualquier/ruta/inventada`, `/recibos` y `/cajas/algo/mas`, los tres 200 con el mismo `sha256`. **Un 200 a secas no habría probado nada** (lección de #6: el servidor de Vite ya contesta 200 a cualquier ruta), así que se afirman el tipo y el cuerpo. Los recursos con huella salen con **una sola** `Cache-Control: public, max-age=31536000, immutable`, y el escudo —que no lleva huella— **sin ninguna**; `gzip` lleva el JS de **292 327 a 101 827 bytes**. **Y ahí hay otra medida que corrige al monolito**: su pareja `expires 1y;` + `add_header Cache-Control "public, immutable";`, levantada en un segundo `server` para no tener que creérselo, emite **dos** cabeceras `Cache-Control` —`max-age=31536000` y `public, immutable`—, de modo que quien lea la primera se queda sin `immutable` y quien lea la segunda, sin la duración. **El criterio 4 y el 6, sobre la imagen reproducida**: `USER 101` resuelve a **uid 101**, distinto de 0 y numérico; y `ls /usr/share/nginx/html` da exactamente `assets`, `escudo-catacaos.png` e `index.html`, con **cero** `node`, **cero** `node_modules` y **cero** archivos de código. **El criterio 8 se ejecutó contra `ghcr.io` de verdad**, con el bucle del trabajo `comprobar` y un `sha` inventado: `kamayuk-caja` y `kamayuk-caja-migrador` contestan **404** y `kamayuk-caja-interfaz` **403** —porque el paquete todavía no existe y un token anónimo ni siquiera se puede acotar a él—, o sea que los **tres desenlaces del archivo están vivos** y el tercero, el que no permite concluir nada, también falla. **Tres cosas más que salieron de medir y no de leer.** (1) `10-listen-on-ipv6-by-default.sh` **no reescribe** nuestro `default.conf`: compara su sha1 con el que `apk manifest` declara para el `default.conf` del paquete (`565a608f…`) y sale en cuanto ve que difiere — el del nuestro no se anota, porque cambia con cada edicion y una cifra que se queda vieja sola es peor que ninguna — el comentario heredado de `sgtm` decía lo contrario y se corrigió. Lo que sí escribe en cada arranque es `30-tune-worker-processes.sh`, con un `sed -i.bak` sobre `/etc/nginx/nginx.conf`, que viene `uid=0 gid=0 mode 644` según las cabeceras tar de la capa; y sin permiso sobre `/var/cache/nginx` no es un aviso sino **`[emerg] mkdir() … failed (13: Permission denied)`** y el contenedor no levanta, reproducido apuntando los temporales a un directorio sin permiso. (2) Dejar `user nginx;` corriendo sin ser root da `[warn] the "user" directive makes sense only if the master process runs with super-user privileges, ignored`, que es por lo que el `sed` lo borra. (3) **El `index.html` que `vite build` emite fija sus recursos en absoluto** —`/assets/index-<huella>.js` y `/escudo-catacaos.png`, porque `vite.config.ts` no declara `base`—, así que el «`server_name` sobre el prefijo `/caja`» que pide el issue **no se puede escribir**: `server_name` casa anfitriones, no rutas, y servir bajo `/caja/` sin quitar el prefijo dejaría al navegador pidiendo `/assets/…` a la raíz del dominio, que es una ruta que la propia regla `PathPrefix(/caja)` de Traefik ya no casa. Se sirve en `/` —que es además lo que los criterios 2 y 3 exigen— y **el prefijo tendrá que quitarlo el ingreso de #17, o cambiar `base`**. `yarn verificar` sigue en **634 de 634**, «`Test Files 20 passed (20)`» con el paréntesis leído, y `yarn build` en verde con sus 70 módulos: este issue no toca `frontend/src/` ni `frontend/verificaciones/` |
| **#17 — el descriptor de despliegue de la interfaz** (`infrastructure/src/descriptor.ts`: la tercera imagen, el `ConfigMap` + `Deployment` + `Service` de `kamayuk-caja-interfaz`, el `IngressRoute` partido en dos rutas con `priority` explícita, el `Middleware` que quita el prefijo y las dos `NetworkPolicy` de la interfaz; `src/nginx-de-la-interfaz.ts` con el `frontend/nginx.conf` copiado carácter a carácter; **17 pruebas nuevas** que dejan la suite en 31; y el `paths:` de `infraestructura.yml`, que ahora nombra los archivos de `frontend/` y de `backend/` que esas pruebas leen) | Quince roturas, cada una sola y restaurada por copia comparada con `cmp`: **(A)** **invertir las dos prioridades**, que es lo que el criterio 5 pide; **(B)** y **(B')** quitarle la `priority` a cada ruta por separado; **(C)** poner el middleware **también** en la ruta de la API; **(D)** quitarlo del todo; **(E)** llamar `kamayuk-caja-web` a la interfaz; **(F)** quitarle su `securityContext`; **(G)** un dígito de la copia del `nginx.conf` (`gzip_min_length 1024` → `1025`); **(H)** darle a la interfaz el `componente` del backend; **(I)** dejar su egreso en `[]`; **(J)** la política de entrada al puerto **80** en vez del 8080; **(K)** declarar `base: "/caja/"` en `vite.config.ts`; **(L)** quitar la interfaz de `imagenes`; **(N)** meterle un `secretKeyRef`; **(O)** montar el `ConfigMap` sobre `conf.d` entero en vez de con `subPath`; **(P)** quitarle a `BarraGlobal.tsx` su ruta absoluta, o sea **la mitad contraria** de (K) | **(A) 1 en rojo**, y es el que este issue existe para producir: «al reves, la API la contesta el nginx de la interfaz con un 200 y el `index.html` dentro: **expected 10 to be greater than 20**». **(B) y (B') 1 cada una**, «la ruta de la API no declara prioridad: quedaria a merced de la longitud de la regla: expected undefined to be defined» y su hermana — hacen falta las dos, porque hoy la precedencia **saldría bien por accidente** (Traefik v3 ordena por longitud de regla) y lo que se protege es que no dependa de eso. **(C) 1**: «quitarle `/caja` a la API deja a Spring buscando `/pagos`: 404 en todos sus endpoints: expected [ 'kamayuk-caja-quitar-prefijo' ] to deeply equal []». **(D) 2**, desde dos ángulos que no se solapan: la ruta sin middleware y el acoplamiento con `nginx.conf` («expected +0 to be 1»). **(E) 7**, empezando por «expected [ 'kamayuk-caja-web', …(1) ] to deeply equal […]» y siguiendo con cinco «no hay ningun Deployment «kamayuk-caja-interfaz»» — el nombre repetido no rompe el despliegue, rompe **todo lo que busca a la interfaz por su nombre**. **(F) 1**, «expected undefined to be true». **(G) 1**, la copia y el archivo dejando de ser el mismo texto. **(H) 3**: «no hay politica de Ingress para la interfaz», «…de Egress…» y «expected […] to have a length of 1 but got 3» — o sea la interfaz heredando **las tres aristas del backend**, incluida la salida a PostgreSQL. **(I) 2**, una de ellas en la prueba de C-17 reescrita: «kamayuk-caja-interfaz-egreso: sin DNS ninguna otra regla suya puede resolver un nombre». **(J) 1**, «expected [ 'TCP/80' ] to deeply equal [ 'TCP/8080' ]». **(L) 3**, incluida la prohibición (b) por su nombre. **(N) 1**, «expected [ { name: 'KAMAYUK_DB_CLAVE', …(1) } ] to deeply equal []». **(O) 1**, «expected '/etc/nginx/conf.d' to be '/etc/nginx/conf.d/default.conf'». **La auditoría de `infrastructure` se ejecutó de verdad, no se razonó**: un arnés no versionado —por lo mismo que los cotejos contra el artboard de #5 a #14: necesita el repositorio hermano clonado al lado— importa `auditarDescriptor` del hermano y le pasa estos manifiestos con las tres `PriorityClass` de la plataforma. **Cero incumplimientos**, y muerde: con la rotura (F) puesta contesta «[caja] Deployment/kamayuk-caja-interfaz, contenedor «interfaz»: sin `securityContext` endurecido…». **Y lo que este issue deja de verdad es que las dos salidas que el enunciado plantea NO son alternativas, medido contra el nginx de la imagen.** Levantando `nginx:1.31.4-alpine` —el binario extraído de las capas reales en #16— con el `nginx.conf` del repositorio y dos `dist/` distintos, las cuatro combinaciones dan: **(A)** sin `base` y sin quitar el prefijo, `/caja/assets/index-<huella>.js` → `200 text/html 1368 B`; **(B)** sin `base` y quitándolo, nginx sirve bien (`200 application/javascript 292 327 B`) pero el navegador pide `/assets/…` **a la raíz del dominio**, que `PathPrefix(/caja)` no casa y que este descriptor no puede reclamar —prohibición (a)—; **(C)** con `base` y sin quitarlo, `/caja/assets/index-<huella>.js` → **`200 text/html 1383 B`**, el `index.html` otra vez: el navegador rechaza el módulo por su tipo y la pantalla queda en blanco, **otro 200 que miente**; **(D)** con las dos, todo correcto —HTML, `application/javascript`, `image/png` y una recarga en `/caja/recibos`—. O sea: el middleware es necesario y no suficiente, y `base` es necesario y no suficiente. **Y hay una tercera pieza que impide cerrarlo hoy**: `src/barra/BarraGlobal.tsx` escribe `src="/escudo-catacaos.png"` en el JSX y **Vite no reescribe un literal de cadena de JavaScript** — construido con `--base=/caja/`, `dist/index.html` pasa a decir `/caja/escudo-catacaos.png` y el paquete sigue diciendo `/escudo-catacaos.png` (`grep -c` sobre el `dist/`: 1 y 0). Declarar `base` hoy **pone rojo el check de Frontend**: medido sobre `vite preview` con Chromium, `mirar.mjs` sale con **código 1 y cuatro «Failed to load resource: the server responded with a status of 404»**, uno por sección, y una sonda de Playwright dice cuál: `404 image http://localhost:5199/escudo-catacaos.png`, con todo lo demás en 200 bajo `/caja/`. Arreglarlo es tocar `frontend/src/` y `frontend/verificaciones/barra.test.tsx`, que este issue tiene prohibido; así que **se hace la mitad que es de este repositorio y la otra queda declarada**, con una guarda que obliga a que se muevan juntas: mientras haya una ruta absoluta en `src/`, `base` tiene que estar sin declarar, y el día que no la haya, `base` tiene que valer `/caja/`. Muerde por los **dos** lados: (K) da «…: expected '/caja/' to be '/'» y (P) da 2, «ya no hay rutas absolutas en `src/`: toca declarar `base: "/caja/"`…» y «expected [] to deeply equal [ '/escudo-catacaos.png' ]». **Cuatro cosas más que salieron de medir.** (1) `vite preview` con `base` **redirige `/` a `/caja/` con un 302**, así que el `curl -sf` de `frontend.yml` sigue saliendo 0 y Playwright lo sigue — el arnés no se rompe por ahí, se rompe por el escudo. (2) La copia del `nginx.conf` va en un módulo aparte y **no se lee del disco**: un descriptor es una función pura, y una lectura dependería además de dónde quede el paquete cuando lo importa el hermano por su `link:`. Lo que la hace segura es la comparación, y para que esa comparación **corra** cuando lo único que cambia es el archivo hubo que añadir `frontend/nginx.conf` al `paths:` del flujo — sin esa línea la guarda no se rompe: **deja de correr, en verde**, que es la lección de #27 aplicada a un `paths:`. De paso: **GitHub Actions no admite anclas de YAML**, así que la lista va escrita dos veces con su motivo. (3) `subPath` deja el archivo de solo lectura, y el arranque de la imagen lo tolera: su `10-listen-on-ipv6-by-default.sh` hace `touch … 2>/dev/null || { entrypoint_log "… (read-only file system?)"; exit 0; }` —leído del guion de la imagen, no supuesto—; el que sí escribe en cada arranque, `30-tune-worker-processes.sh`, toca `/etc/nginx/nginx.conf`, que este montaje no cubre. (4) La política de entrada abre el **8080 del pod** y no el 80 del `Service`: una `NetworkPolicy` filtra sobre el puerto del contenedor, y con 80 no admitiría nada. **Y se retiró una aserción que no podía fallar**: `expect(NOMBRE_DE_LA_INTERFAZ).not.toBe(NOMBRE_DEL_BACKEND)` compara dos constantes del propio archivo de pruebas. **Y se retiró una afirmación falsa de la cabecera del descriptor**: decía «los `Deployment` apuntan a imágenes que **aún no existen**… y no se despliega nada», y no es cierto desde el flujo D. Medido contra `ghcr.io` con el mismo bucle del trabajo `comprobar` y el `sha` de `main` (`11e5a51c`): `kamayuk-caja` **200**, `kamayuk-caja-migrador` **200**, `kamayuk-caja-interfaz` **200**. Las pruebas pasan de **14 a 31**, «`Test Files 1 passed (1)`» con el paréntesis leído, y **3 corridas con `--sequence.shuffle`, 31 de 31**. `cd infrastructure && yarn verificar` en verde: lint, tipos y pruebas |
| **#18 — el servicio `caja-interfaz` del compose, la documentacion y el hueco declarado** (`despliegue/compose.yaml`: el cuarto servicio, con `build.context: ../frontend`, `target: interfaz`, el puerto publicado por variable y **sin `depends_on`**; `docs/00-gobierno/huecos-en-infrastructure.md`, que nombra lo que queda en el repositorio hermano; y `CLAUDE.md`, `README.md` y `docs/D0-desarrollo/` puestos al dia con salidas ejecutadas) | **Esta maquina no tiene Docker** —comprobado yo mismo: `docker`, `podman`, `nerdctl`, `buildah`, `docker-compose` y `podman-compose` los seis «command not found»—, asi que **el criterio 2 no se ejecuto con `docker compose up`, y queda dicho**. Lo que si se pudo: el **criterio 1 se ejecuto de verdad**, bajando el binario oficial `docker/compose v5.5.1` de GitHub (sha256 comprobado contra su `.sha256`) — `docker compose config` **no necesita demonio**, solo interpreta y sustituye. Y para el 2 se repitio lo de #16: las **ocho capas** de `nginx:1.31.4-alpine` bajadas de `registry-1.docker.io` (digest amd64 `sha256:1f25fedd…`) y su binario ejecutado con su cargador musl —`nginx version: nginx/1.31.4`— sirviendo el `dist/` real con el `nginx.conf` del repositorio, del que solo se reescriben **dos lineas**, `listen` y `root`, con un `diff` que no ensena ninguna mas. Cuatro roturas, cada una sola y restaurada por copia comparada con `cmp`: **(A)** quitar el servicio entero; **(B)** anadirle un `depends_on` del backend; **(C)** llamar a la variable del puerto `KAMAYUK_PUERTO_INTERFAZ`, que es lo que el issue pedia literal; **(D)** dejar `dist/` sin `index.html`. | **(A)** `config --services` pasa de cuatro a **tres** —`caja-migraciones`, `caja-implantacion`, `caja`— **con `exit=0`**, y ahi esta la mitad que ensena: «`config` resuelve sin error» es la primera mitad del criterio 1 y **no puede ver** que falte un servicio; lo que lo ve es contar. Y la reproduccion de la guarda de `infrastructure` pasa de **3 rojos a 2**, o sea que el tercero es exactamente lo que este PR anade. **(B)** el criterio 3 en rojo —«el servicio declara depends_on {'caja': {'condition': 'service_healthy', 'required': True}}»— **con el criterio 1 entero en verde**, los cuatro servicios listados: son dos afirmaciones que no se cubren. **(C) es la rotura que cambio el codigo**, y su rojo no es una asercion sino una cifra: con el `.env` **real** de la plataforma delante —`infrastructure/despliegue/.env.ejemplo`, que trae `KAMAYUK_PUERTO_INTERFAZ=8081`, y cuyo README dice «El `.env` es el mismo de la plataforma»— `${KAMAYUK_PUERTO_INTERFAZ:-8082}` resuelve a **8081**, que es el puerto que ya publica la interfaz del monolito (`infrastructure/despliegue/compose.yaml:252`). O sea que el criterio 2, escrito con ese nombre, **documenta un puerto al que nunca se llega**: el valor por omision no se aplica jamas, y las dos interfaces se pelean el mismo puerto del anfitrion. Con `KAMAYUK_PUERTO_INTERFAZ_CAJA` resuelve a **8082** con el mismo `.env`. **(D)** `GET /` da **404** y `curl -sf` sale **22**, que es lo que hace que el 200 del criterio 2 signifique algo. **Las medidas del criterio 2, contra el nginx de verdad**: `/` da `200 text/html` de **1368 bytes**, `Cache-Control: no-cache` y cuerpo **identico byte a byte** a `dist/index.html` (`cmp` en 0); `/assets/index-MIhEBA89.js` da `200 application/javascript` 292 327 B con `public, max-age=31536000, immutable`; el escudo, `200 image/png` 196 608 B **sin `Cache-Control`**; y una ruta inventada, `200 text/html` de 1368 B. **Y de paso quedo medido el 200 que miente**, que es por lo que este servicio publica un puerto en vez de colgar de Traefik: `GET /caja/assets/index-MIhEBA89.js` devuelve **200 `text/html` de 1368 bytes** —el `index.html`, no el JavaScript—, o sea #37 reproducido desde este lado. **Tres afirmaciones del encargo salieron falsas al medirlas, y estan corregidas con la medida delante.** (1) «Subir la `version` del descriptor fijado de `caja` en `infra/descriptor/sistemas.ts`, o la interfaz no aparece en el cluster»: **falso**. `manifiestosDe(caja, ENTORNO)` ya emite **12 manifiestos** con `kamayuk-caja-interfaz` dentro sin tocar ninguna version, y el cuerpo de `componerDescriptores` (`infra/descriptor/index.ts`) **no contiene la palabra `version` ni una vez** —desestructura `for (const { descriptor } of fijados)`—; ademas la dependencia es `link:`, un enlace al arbol de fuentes del hermano, como su propia cabecera explica. Para saber que esa medida muerde se cambio el nombre buscado por uno inventado: «expected [ …(12) ] to include 'kamayuk-caja-inexistente'». Lo que **si** es cierto queda escrito: ese campo es hoy documentacion que nadie compara, y el dia que ADR-0031 lo haga gobernar arrastrara un valor que ya era mentira. (2) «Las guardas que cuentan servicios y manifiestos se ponen en rojo»: son **tres** archivos y no dos, y **dos de los tres rojos ya estaban vivos desde #17**, no los trae este PR. Reproducidos con el codigo de `infrastructure` —no se pudieron ejecutar, ver (4)—: «expected [ 'caja', 'caja-migrador', …(1) ] to deeply equal [ 'caja', 'caja-migrador' ]» (`despliegue-de-los-sistemas.test.ts:186`), «expected [Function] to not throw an error but 'Error: «caja: despliegue» no tiene ex…' was thrown» (`compose-de-los-sistemas.ts`, `principalDe`) y «expected [ 'caja', 'caja-implantacion', …(2) ] to deeply equal [ …(1) ]» (`compose-de-los-sistemas.test.ts:83`). **El segundo es el que hay que arreglar primero**, y no por ser el segundo: **lanza** en vez de afirmar, y una excepcion tira el archivo entero, asi que tapa los hallazgos de esa guarda **para los cuatro sistemas**. Se comprobo ademas el que **no** se rompe: `imagenes-publicadas.test.ts:154` compara sistemas, no imagenes, y sigue en cuatro. (3) «`Identidad.ts`, el *client* publico de Keycloak»: ese archivo **no declara ningun cliente**, los lee del realm versionado (`realmSgtmJson()`, linea 196). Contados: `despliegue/identidad/realm-sgtm.json` trae **dos**, `sgtm-backoffice` (publico, PKCE `S256`, redirecciones a `:5173` y `:8081`, las del monolito) y `sgtm-verificacion` — **ninguno de esta interfaz**, y ni `:8082` ni `<dominio>/caja/*` aparecen. (4) **Y el impedimento del entorno, ejecutado**: `vitest run` sobre las dos guardas da «`Test Files 2 failed (2)` · `Tests no tests`» con «Cannot find module '../../../normativa/infrastructure/src/descriptor' imported from …/infra/descriptor/sistemas.ts» en la linea 21 — se cae **al cargar**, o sea que no se pone roja diciendo que falta: no llega a mirar nada. Salida: clonar `normativa`. **Y encima de eso hay una trampa de `git worktree` que costo una medida**: alli `infrastructure/node_modules/@kamayuk/infra-contrato` es un enlace **relativo** que apunta fuera del clon, y `yarn typecheck` saca **48 `TS7006`** y **2 `TS2307`**; el `TS2307` nombra la causa y sale el primero, pero quedan dos lineas ciertas bajo cuarenta y ocho que hablan de parametros sin tipo. **Una decision de nombre, con su motivo medido**: el servicio se llama `caja-interfaz` y no `interfaz`, por la misma regla que este archivo ya escribe para el backend —los cuatro composes comparten `kamayuk-plataforma` y Compose da a cada servicio un alias con su nombre—. Hoy `interfaz` no lo declara nadie mas en esa red (la plataforma trae `base`, `correo`, `identidad`, `ingreso`; `rentas` y `catastro`, sus tres procesos cada uno) y el `interfaz` del monolito vive en el proyecto `sgtm`, que **no declara `networks`** y por tanto no comparte red; pero ADR-0030 §1 promete **cuatro** interfaces, y el nombre generico se corrige en un repositorio ahora o en cuatro despues. **Y una guarda muda encontrada al pasar**: `RUTAS_DE_CODIGO` de `verificar-fila-del-registro.mjs` son `backend/*/src/main/`, `infrastructure/src/` y `frontend/src/`, asi que **este PR no habria necesitado fila** — un compose es configuracion de produccion y la guarda no lo mira. La fila se escribe igual; ampliar la lista es una decision de otro issue, y se declara en vez de hacerse a medias. `yarn verificar` en verde por los dos lados: `infrastructure/` **31 de 31** y `frontend/` **634 de 634** en 20 archivos, con el parentesis leido, y los cuatro arneses de navegador pasando —salvo el primer arranque de `pegajosa`, cuya intermitencia queda medida y explicada en DEV-02 §7: con la cache fria la tipografia llega **entre** las dos medidas y desplaza la maqueta 3 px; tres corridas seguidas despues, verde—. Este PR **no toca `frontend/`**: `git diff origin/main -- frontend` esta vacio |
